# Controlled semantic-test connection — unexecuted

Connects existing address parser and county rules to the one-shot runner. reviewGoogleServiceArea shares the prior evaluator logic without asserting fixture provenance; all realVerificationAccepted/admission/booking/delivery flags remain false. Existing fixture wrapper output is preserved.

scripts/run-google-semantic-inspection.mjs is a separately invoked test command requiring the existing explicit approval/window arguments and private SEND consent. Uses fixed packet google-semantic-inspection-002, one request and USD 0.10 application allowance. It requires the old connectivity claim to exist with matching project/packet/count/liability, correct file ownership/modes, no symlinks and no extra entries. Old claim is never removed. Runner creates the second claim before dispatch. No automatic reset/retry.

Private input and approved login are reused. Response is parsed in memory; only fixed address status and coverage enum are shown through a private native dialog after timely transport completion. No raw fields, correction candidate or response ID are displayed/logged/stored. stdout remains transport outcome and false authority. Dialog cancellation does not refund/retry a completed request. Provider-derived summary is discarded; no durable semantic acceptance receipt.

Inspection uses explicit LOCAL review bindings, not a real tenant/session proof or deployed runtime identity. A REVIEW/IN_AREA result is not job admission. CORRECTION_REQUIRED/UNKNOWN does not trigger a follow-up request. Real corrections/responseId lifecycle and retention remain separate. No claim that every physical address is supported; H-only record and required optional metadata remain conservative.

Review scripts/google-semantic-review.test.mjs and existing one-shot/window/input tests, plus google-service-area.spec.ts compatibility case. Actual live semantic request is NOT performed in this checkpoint. No deployment, configuration/IAM/key, customer/job/calendar/payment or SMS action.

Validation: 12 script tests passed; 112 runtime suites / 2,151 tests passed with three existing skips. Lint, build/Prisma generation, architecture, syntax, whitespace, cross-repository consistency and eight governance regressions passed. No UI change or browser QA claim; private live-result presentation remains unexercised.

Next: explicit owner approval for ONE new semantic request, USD 0.10 local allowance and fresh 15-minute window, including private result presentation without retention. Recheck existing claims and approved account at run time. No new generic parser or connectivity rehearsal. APP-013/2B Now; walkthrough 3/8 (37.5%) unchanged.
