# P06/R11 enabled22 bounded local investigation

## Investigation card before fixture work

Authority: owner `proceed` after the enabled22 refusal-stage result. Source runtime `574f25a`, backend documentation HEAD `3a78a38`; APP-013/2B and P06/R11 acceptance remain the same protected phone/address/reviewed-submit journey creating one job. This is diagnosis only, not permission for a repair or live action. Missing behavior is an authoritative explanation of the observed `CURRENT_VERIFICATION_UNAVAILABLE` response.

Reuse `DurableVerificationService`, `TwilioVerifyAdapter`, `ConversationMemoryCipher`, the controlled proof validators, and the existing controlled-phone-proof unit fixture seams. Finite checklist: map stage to predicates; reproduce direct successful CHECK and reconstruction of the service over the same encrypted synthetic ledger; exercise mismatched policy, revoked proof and expired proof as negative controls; record what is ruled out and what requires real evidence. Temporary script in `/private/tmp`, imports existing compiled source; no application interface/code/schema changes. Command: `node /private/tmp/r11-enabled22-local-proof-check.cjs`.

Synthetic identities, AES key, clock, ledger and provider responses only; database ports are in-memory functions. No real participant, credential, network, database, provider, runtime LOGIN, deployment, hold reset or browser action. Concurrency/recovery coverage is limited to service reconstruction and existing lock seams, not a database race claim. Existing connected browser results are historical, not rerun or promoted to current evidence. Finish: reproducible predicate results or an explicit evidence gap, not another live attempt. Disabled state stays verified; no rollback action needed for disposable local files. Implementer owns investigation; any indispensable live data read or material repair needs a concrete separate decision. No scope deviation.

## Completed local result

Six synthetic checks passed using the existing compiled implementation: direct single CHECK yields current proof; a new service/cipher instance recovers current proof from the encrypted ledger after a three-minute delay; mismatched business policy, missing proof on an approved receipt, sticky revocation and exact expiry all refuse. Two synthetic adapter invocations; zero live calls. This excludes those simple paths as a reproduced defect, not as proof about the real session. No production change is justified yet.

## Consolidated diagnostic preparation card

Owner continuation authorizes local preparation only. Reuse the existing hidden TTY/READY/anonymous-pipe handoff, fixed child database identity, compiled AES-GCM cipher, controlled proof validator, immutable enabled22 packet and existing job/address readback SQL. Prepare a private helper in temporary local storage; installation and execution remain unapproved. One future repeatable-read read-only connection will resolve exactly one phone hold for the fixed tenant/packet, then read only that conversation's lifecycle/encrypted verification ledger, bounded verification/invalidation audit metadata, exact request job count and address stage. One exact revision metadata read must confirm image and encryption-key version before one conditional secret-version read. Decode only that ledger in short-lived process memory and evaluate proof/policy/scope/deadlines against the recorded refusal timestamp.

No raw phone, address, code, token, ledger, SID, HMAC, customer text, ciphertext or key enters stdout, saved results or repository. Outputs are fixed classifications, counts, booleans and timestamps; post-event changes or missing/purged data remain inconclusive. Preserve current revoked approvals, expired sessions and all holds; never invoke admission/provider code against the live DB. Frozen baseline and docs/architecture/whitespace checks apply; synthetic tests cover good/missing/mismatched/revoked/expired proofs, post-event changes, no-job/address stage and output privacy. A source/hash-bound review and explicit owner approval must precede any installation, connection or secret read. No repair, instrumentation, image build or new runtime packet is included. No scope deviation.

## Prepared result

The consolidated helper is locally prepared under `/private/tmp/r11-enabled22-forensics`; no private-volume installation, authorization or attempt exists. It reuses exact source hashes, fixed packet/request scope and the existing cipher/proof code. Syntax and actual `--review` pass without external action. Eight synthetic analyzer tests pass, including output privacy and later-state ambiguity. Plan `790763ef-1f2a-44b7-a690-56bfee9fba9a` proposes one 8:30–9:15 AM Eastern diagnostic with a single hidden password entry, fixed revision metadata read, one read-only PostgreSQL connection/rollback and conditional exact encryption-key version read. This broader privileged read is explicitly unapproved; see governance `APP013_P06_R11_ENABLED22_RETAINED_STATE_DIAGNOSTIC.md`. No application repair, speculative gate weakening or new browser test.

## Approved installation; execution pending

Owner approved plan `790763ef-1f2a-44b7-a690-56bfee9fba9a` for September 23, 8:30–9:15 AM Eastern. Six private files installed under `/Volumes/Signmons-P06/r11-retained-state-diagnostic-20260923-0830`, including exact hash-bound authorization. Inventory, ownership, 0700 directory/0600 files, byte equality with reviewed preparation and installed no-action review passed (`R11_RETAINED_STATE_PREPARED_NO_ACTION`). No attempt, database connection, revision read or secret read occurred. Owner hidden-input execution at or after 8:30 AM is pending; stop results must not be retried. Application runtime source remains unchanged; no root cause or admission outcome is established. No scope deviation.
