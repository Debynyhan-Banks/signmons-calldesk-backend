# 2B source-mapped implementation plan

Planning-only inspection at backend 76d48e1. Governance APP013_2B_IMPLEMENTATION_CARD.md is the detailed linked card under the unchanged frozen execution contract. No scope deviation.

Verified: current-proof-admission and freshness contracts are fixture-only; staging-phone constructs durable verification without freshness policy and grants no job admission; protected operator intake controller is not registered; Google operation/ledger composition remains fixture-only; transient CLI screen discards selected input. Existing encrypted ledger, session locks, customer browser transport, admitReview transaction, address ledger and shared parsers are reuse points, not a reason to rebuild them.

The card defines controlled provenance, same-session draft/correction/revalidation, transaction-time consumption, proposed field ownership/lifetimes, four finite internal work items with file names, positive/negative/race/recovery/browser tests and separate execution gates. It does not authorize treating mocks as real evidence or auto-activating routes. Retained Google-derived evidence permission and precise customer bootstrap/runtime identity remain unresolved. Therefore the first 2B contract item is not yet marked complete and implementation must wait for those decisions, not be replaced with another demonstration.

Validation for this Markdown-only pass: frozen baseline, cross-repository docs consistency, 17 governance regressions, backend architecture and whitespace checks. No new runtime/build/browser/live-provider test claimed. Main safeguards merge remains separate from unmerged APP-013 application work. Walkthrough accepted remains 3/8 (37.5%), not total MVP completion.
