# P06-R02 recovery policy correction — execution blocked

Owner approved preparation using the proposed six-hour policy, with separate approval for any destructive recovery. No credential, connection, migration, restore, snapshot or provider change was authorized or performed in this turn.

Fresh official-document inspection on 2026-09-15 found a material contradiction in the earlier recommendation. https://neon.com/docs/manage/branches generically lists restoration to a branch's own history, but its linked specific guide https://neon.com/docs/guides/branch-restore repeatedly states instant restore/PITR is supported only for root branches and historical sources must also be roots. The guide also contains parent-restore examples; those do not establish child self-history recovery. The earlier console source was production, not the child's own history. Therefore six-hour retention alone is NOT verified recovery of br-sparkling-sun-ay6gr5e8's pre-migration state. Prior R02 evidence/recommendation is superseded on this point. Do not run a migration or reset from parent based on it.

The existing child p06-isolated-staging-v1, endpoint ep-jolly-flower-ayc6w9hv, remains unchanged. No root conversion, replacement branch, logical dump or added storage is silently selected. An independently recoverable root target or private logical-backup/restore design would change the approved recovery mechanism and needs owner direction plus exact cost/privacy/restore validation. No test records may be discarded to bypass this gate.

Credential review also confirms that ordinary table GRANT privileges alone cannot authorize ALTER of existing SmsConsentRecord and PropertyAddress. PostgreSQL18 https://www.postgresql.org/docs/18/ddl-priv.html assigns alteration rights to owners or eligible owner-role members. Neon https://neon.com/docs/manage/roles says console/API-created roles automatically receive neon_superuser membership; SQL-created roles require explicit grants. Granting the inherited owner role could transitively broaden privilege, so it is not a proven least-privilege shortcut. Exact object ownership/membership, migration-history writes, REFERENCES and schema/type/function privileges require a bounded synthetic role rehearsal before claiming a credential packet executable. No role SQL executed or password generated.

Provider-directory CLI returned unknown command; official Neon Markdown was read directly after the web reader rejected its content type. No tool installation. Source entry backend2906487, governance4f8f121; focused remotes fetched, worktrees clean before documentation edits; saved unrelated changes preserved.

R02 remains open; R01 closed, R02-R12 open (11), added0. Accepted5/60 (8.3%), walkthrough3/8 (37.5%); ETA unvalidated. Proposed deviation: replace the unsupported child-PITR assumption with a separately approved recoverable-target or logical-backup method. No deviation implemented. Owner decision needed before further execution preparation that assumes one of those methods.

Validation: documentation-only; frozen/full consistency, governance regressions, architecture and whitespace checks. No new runtime tests, browser recovery proof or build claimed.
