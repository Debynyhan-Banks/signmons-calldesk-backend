# P06 / R11 fixed address-capacity diagnostic preparation

The owner requested `proceed` after the enabled19 pre-reservation result and proposed bounded address-capacity check. Governance card `APP013_P06_R11_ADDRESS_CAPACITY_DIAGNOSTIC.md` records the exact criterion, inspected source, fixed account/tenant, retained fields, finite checklist and approval boundary before implementation.

The inert helper `scripts/p06-r11-address-capacity-diagnostic.mjs` accepts an already-connected query client only. It verifies the current transaction is read-only, then performs one parameterized aggregate for the fixed address account and tenant. Counts include all operation states, matching AddressOperationLedger; account and tenant ceilings remain two operations/200000 micros, with 100000 micros for a new operation. It returns only count/cost totals, invalid count, capacity booleans, bounds and a classification. Invalid shape, negative/unsafe/inconsistent values or unexpected states refuse. It imports no database client, credentials, network or provider implementation; direct invocation has no action.

Nine focused tests pass for zero/below/exact/over-limit counts and costs, other-tenant account consumption, malformed/unsafe/inconsistent results, read-write refusal before query, no query retry and inert invocation. Three existing hidden-input transport pseudo-terminal tests pass. The initial direct-invocation test found URL-encoded spaces in its test path; using fileURLToPath corrected the harness and the actual direct invocation passed. Architecture, governance and whitespace checks pass. These are local mock/transport checks, not a live database or SQL execution result.

The private caller will reuse the previous diagnostic's one-use authorization/attempt, source/hash/window binding, fixed target identity/PostgreSQL 18 checks, hidden input, repeatable-read read-only transaction, rollback and redacted errors. Fresh operation and exact execution approval are required. A current aggregate cannot prove which historical transient error occurred; capacity available leaves other gates unresolved. No application path, schema, policy, hold, database state or provider was changed. P06 remains 12/14 with R11/full R12 open. No scope deviation.
