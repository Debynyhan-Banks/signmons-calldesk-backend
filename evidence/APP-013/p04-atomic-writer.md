# P04 — atomic controlled writer checkpoint, replay pending

Owner: proceed. Entry backend 640a0f3/governance 0c6941f; origins fetched, focused branches matched and PR21 open. Saved unrelated changes preserved. The source card recorded this bounded transaction before coding. Existing 2B/P04 acceptance served: explicit customer submit and trusted current policy/verification must create exactly one job atomically, with no downstream authority. No scope deviation or new package.

## Changes

- CustomerIntakeContinuationService.submitControlled uses the strict copied v2 submission, actual current reader and P03 verification callback. Closed/safety/stale input refuses before the verification factory. The short final transaction checks current evidence, reader and authority, records CUSTOMER submission audit, and invokes shared persistence. It then awaits the same-transaction post-write freshness guard before commit. Unknown failures never report success.
- Shared persistAdmission retains the legacy operator branch. Controlled branch uses null operator/review inputs, exact v2 intakeAdmission allowlist, SYSTEM_AI signmons-intake-admission-v1, DETERMINISTIC_POLICY and STANDARD operational default explicitly not a safety assessment. No humanReviewed=true on controlled results. Customer, null-coordinate customer-stated property, job, link, consent binding, audit and conversation completion share one transaction. No schema migration.
- P03 now supplies a request-local beforeCommit callback bound to the transaction that checked the evidence. It rechecks authority, database time, original deadline, captured phone expiry and customer credential expiry without rereading a completed session as a new submission or performing provider I/O. Wrong transaction, revocation, expiry or use after return refuses. No renewed deadline or persisted provider payload.
- Public controlled success exposes only status/requestId/jobId/state/jobCreated and false payment/booking/dispatch/delivery authorization. No route/DI/live composition registration. Exact committed replay is NOT implemented: closed duplicates safely refuse and cannot create another job. Do not activate until P04/P05/P06 exits are satisfied.

## Validation

Seven new unit cases: controlled actor/record allowlist, post-write refusal ordering, closed/safety preflight, P03 same-transaction/lifetime guard and deadline/revocation. Full backend result: 116 suites passed, 1 skipped; 2,263 tests passed, 3 skipped. Build/lint/architecture and full/production audits passed, zero findings. Full governance baseline/consistency, all 21 regression tests and both whitespace checks passed at final closeout. Final database/browser rerun exited zero and cleaned the disposable database.

Existing organization harness extended in verify-operator-intake-admission.mjs using actual disposable local PostgreSQL, actual reader/authority/credentials/persistence and EXPLICITLY INJECTED verification callbacks. It proves v2 field allowlist, SYSTEM_AI, granted consent binding, null location, one successful transaction under concurrent duplicate requests, repeat refusal, and audit/consent/post-write guard failures rolling back all counted records and keeping the conversation ONGOING. This persistence proof is not actual phone/Google verification, full P03-to-writer integration, exact receipt replay or live acceptance. Existing legacy browser/operator replay tests rerun as regressions only. No provider calls.

Reproduce: npm run build; npm run lint; npm test -- --runInBand; node scripts/architecture-check.mjs; npm audit --omit=dev; npm audit. Database/browser: ORGANIZATION_EVIDENCE_DIR=/private/tmp/signmons-p04-writer-evidence.geztXA and PLAYWRIGHT_MODULE pointing to installed local playwright/index.mjs, node scripts/verify-organization-profile.mjs. Full run log /private/tmp/signmons-p04-writer-db.log; backend test log /private/tmp/signmons-p04-writer-tests.log. Harness restricts database to random calldesk_org_<hex> on local Unix socket and cleans it afterward; no cloud DATABASE_URL. Existing pg concurrency deprecation warning remains nonblocking. No UI source changes; existing desktop/mobile regressions only.

## Remaining and review

Review submitControlled, both persistAdmission branches, P03 beforeCommit and the new tests/harness block. Next observable result in this SAME P04: authenticated exact committed receipt before provider work, changed-input refusal and lost-response/restart recovery, then full controlled source-to-writer evidence. Existing finite card remains authoritative. P05 route/browser composition and P06 separately approved live run are unchanged. Implementer owns remaining code/tests; owner remains release approver. No external blocker for that code work.

P04 ACTIVE, no completed timing sample. Accepted walkthrough 3/8 (37.5%); remaining-plan 2/60 (3.3%), not whole-MVP percentage. Original low-confidence 60–84 productive-day baseline unchanged. First recorded clock this resumption 2026-09-13 15:25:21 UTC; initial validation 15:30:46 UTC, final rechecks/docs/Git additional. These partial timestamps omit earlier inspection and are not a complete package duration. No merge, deployment, real customer/job/appointment mutation, live sending, charges, configuration or secrets changes. Rollback leaves composition absent or reverts the focused commit.
