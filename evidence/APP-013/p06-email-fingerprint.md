# APP-013 / 2B / P06 approved email fingerprint

2026-09-14. Owner approved the dedicated fingerprint proposal with "yes, i approve". Source backend 06558e2 / governance f7e0c44. No scope deviation. Disabled implementation only; no actual keys generated, fetched, configured or rotated.

## Implemented

- email-consent-fingerprint.ts/spec: dedicated injected 32-byte HMAC-SHA-256 key, copied into private storage. Exact JSON serialization of purpose, version, tenant and existing normalized mailbox. No local-part rewriting; no email verification, consent or send authority. Only internal digest/keyVersion result. Absent/invalid configuration refuses; retire zeroizes the private copy and refuses permanently.
- controlled-intake-runtime-config.ts/spec: required fingerprintKey numeric reference and fingerprintKeyVersion; unknown/missing/invalid values refuse. Reject another purpose's secret resource even at another version. Different reference names cannot prove independently generated bytes: loader/provisioning review must still ensure that.
- scripts/verify-operator-intake-admission.mjs: connected local test uses the actual adapter with synthetic bytes instead of its prior inline fingerprint function. No existing records or production keys touched.

## Validation

- Focused configuration/fingerprint suites: 10 tests passed. Deterministic serialization, tenant/version/key separation, purpose separation from legacy fixture serialization, unchanged mailbox semantics, copy isolation/private serialization, retirement and malformed authority/input covered.
- Full Jest: 126 suites passed, one skipped; 2,336 tests passed, three skipped.
- Build, lint, architecture and Prisma validation passed. Production-only and full npm audits: zero findings.
- Full guarded disposable PostgreSQL and connected 390/1440 browser suite passed with synthetic provider ports. Existing consent append-only/transactional binding and rollback checks retained; shared limits and phone-liability concurrency/restart checks passed. No live provider calls. Existing pg concurrent-query deprecation warning remains non-failing.
- Local logs /private/tmp/signmons-p06-fingerprint-{build,lint,tests,db}.log; artifacts /private/tmp/signmons-p06-fingerprint-evidence. Browser proof still prepares phone evidence before the browser flow, so does not close P06 phone-code journey acceptance.

## Review / remaining

Review adapter private key storage and retire behavior, HMAC serialization, required envelope fields and alias checks; run the commands in the runtime card. Governance frozen/full consistency, 21 regression tests and whitespace gates are required before push.

Fingerprint implementation blocker resolved. Remaining same three P06 items: finish loader/current readers and actual same-page phone-code/close-to-job flow; separately reviewed release packet; separately capped live run and owner closeout. Main stays unbound. No release, IAM, secrets, billing, migration, real customer data or main merge.

Accepted remains 5/60 packages (8.3%), walkthrough 3/8 (37.5%). Provisional 4–8 weeks at 25–30 collaborative hours/week plus external waits remains low confidence; next timing review at ten accepted packages.
