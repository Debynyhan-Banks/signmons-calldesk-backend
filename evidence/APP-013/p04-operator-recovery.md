# P04 operator recovery — one remaining integration item

Final closeout: governance frozen baseline, full cross-repository consistency, all 21 regression tests and both whitespace checks passed.

Owner requested actual continuation. Entry backend 76ff1a4/governance 0ed2e7c; origins fetched, saved changes preserved. Existing P04 recovery card recorded before coding. No scope deviation or new section.

Implemented CustomerIntakeContinuationService.readControlledReceipt({requestId}). Uses existing operator() owner/admin/dispatcher, tenant and impersonation guards. Finds one tenant-owned committed v2 job, locks its original session in existing operator receipt-only mode, re-reads unchanged records, and validates exact v2 metadata, conversation link and required consent binding. Returns minimal receipt and false downstream authority. No customer token, provider, current policy check, write, credential renewal or new route. Unavailable records fail closed. Exceptions are sanitized.

Validation: six new unit cases for malformed/extra-token input, wrong roles and absent/ambiguous records. Full backend: 117 suites passed, 1 skipped; 2,275 tests passed, 3 skipped. Build/lint/architecture and full/production dependency audits passed, zero findings. Existing disposable PostgreSQL harness proves owner/admin/dispatcher receipt access with an operator instance whose customer credential accessor throws; zero credential accesses. Customer token rejection at exact expiry is separately asserted. Wrong roles, foreign tenant, impersonation, extra token, missing request and malformed stored actor refuse. Receipt calls do not change record counts or invoke providers. A deliberately mutated fixture record is restored; these fixture setup writes are not receipt writes. Existing customer/operator desktop/mobile regressions and transactional recovery tests passed. Initial impersonation fixture used the wrong context-setter argument; corrected it to the documented second argument without changing the application guard. Final harness exited zero and cleaned its temporary database. Existing pg deprecation warning is nonblocking.

Reproduce npm run build; npm run lint; npm test -- --runInBand; node scripts/architecture-check.mjs; npm audit --omit=dev; npm audit. Existing node scripts/verify-organization-profile.mjs with ORGANIZATION_EVIDENCE_DIR=/private/tmp/signmons-p04-operator-evidence.Aj4epw and PLAYWRIGHT_MODULE pointing to installed local playwright/index.mjs. Logs /private/tmp/signmons-p04-operator-tests.log and /private/tmp/signmons-p04-operator-db.log. Harness uses random calldesk_org_<hex> local Unix-socket database, not live DATABASE_URL. Final governance consistency/baseline/21 tests and whitespace checks required before commit.

Review readControlledReceipt and its unit/harness tests. ONE P04 item remains: full controlled verification-to-writer proof, then final review. The database writer still uses explicitly injected verification callbacks; no full real-source proof claimed here. P05 route/browser registration and P06 approved live run are unchanged. P04 ACTIVE, accepted walkthrough 3/8 (37.5%), remaining-plan 2/60 unchanged; no overall MVP percentage or completed timing sample. Original low-confidence 60–84 productive-day baseline unchanged. Validation clock 2026-09-13 15:49:22 UTC, not a complete turn duration. No live actions or external blocker for remaining implementation. Rollback leaves controlled registration absent or reverts the focused commit. No merge, deploy, production migration, charges, IAM/secrets/provider or real customer changes.
