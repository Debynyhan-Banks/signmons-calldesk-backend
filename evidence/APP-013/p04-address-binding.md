# P04 active — approved address contract checkpoint

Owner approved the narrow versioned submission change with “yes i approve” after entry inspection demonstrated the mismatch between legacy draft.address:string and P03's structured request. Source backend a4ef398/governance 59b6f80; both origins fetched, focused checkouts clean at entry, saved unrelated work preserved. Approved amendment recorded BEFORE implementation in governance APP013_P04_SOURCE_CARD.md. No additional scope deviation.

Implemented controlled-intake-submission.ts/.spec.ts: exact version2 envelope, session token syntax bounds, request UUID, transcript revision 1–20, confirmed=true, unchanged seven-field draft, and confirmedAddress={street,unit,city,postalCode}. Reuses shared address input bounds. Canonical customer display is street[, unit], city, OH postalCode and MUST match draft.address; no guessing a free-text address, fixture conversion, automatic confirmation or provider-content promotion. Country/state fixed to the approved US/OH path; county eligibility is still a separate P03 check. Controlled phone format is +1 NANP; possession/eligibility is NOT established by syntax. Unknown fields, false confirmation, mismatched components and client proof/tenant/urgency fields refuse. Returned input is copied/frozen.

This new validator is not registered or connected to the submit/job path. Legacy validator and operator paths are unchanged. No database schema, stored data, provider transport, UI, runtime configuration or external service changed.

Validation this run: 17 new tests; full Jest 116 suites passed, 1 skipped, 2,241 tests passed, 3 skipped. Lint, generated-client build, architecture, dependency audits, governance frozen baseline/full consistency/21 regressions and both whitespace checks passed. Database/browser QA is not applicable to an unconnected pure input validator; no prior database/browser result is represented as new. The remaining P04 transaction must still run its own database tests.

P04 checklist: approved contract/validator DONE; actual current-draft and policy loader, atomic job/customer/property/link/consent/audit persistence, safety interruption, exact committed receipt replay, duplicate/race/rollback/recovery proof PENDING. No package split or additional section. P04 remains ACTIVE, not review-ready as a whole; this checkpoint is reviewable code for the approved amendment. No new acceptance or completed timing sample; walkthrough 3/8 (37.5%) and remaining-plan accepted 2/60 unchanged. Continue the remaining P04 card, not P05 or another ticket. Provisional forecast unchanged.

Timing: P04 entry inspection began 2026-09-13 14:56:33 UTC; resumed after approval at 15:00:27 UTC. The intervening inspection/approval interval is not separately attributable to work versus waiting. This resumed implementation/local-gate checkpoint reached 15:04:51 UTC (4m24s), with documentation and Git closeout additional. Keep total package timing open through full P04 acceptance-ready implementation; do not average this partial work as a completed package.

Review the new validator/spec and approved old/new contract in APP013_P04_SOURCE_CARD.md. Reproduce with npm test -- --runInBand controlled-intake-submission, npm run lint, npm run build and npm test -- --runInBand. Cross-repository gate: SIGNMONS_GOVERNANCE_REPO=/private/tmp/signmons-2b-bootstrap-gov node scripts/check-governance-baseline.mjs. Governance consistency uses SIGNMONS_BACKEND_REPO=/private/tmp/signmons-2b-bootstrap-backend. No merge, deployment, production migration, live traffic, charges or customer changes. Rollback: leave the module unused or revert this focused commit; no production rollback required.
