# P04 active — current submission reader

Owner said proceed. Entry backend 9e92078 / governance b9e875d, both focused origins fetched and matched; PR21 remains open. Saved-checkout unrelated edits preserved. The source card was extended before implementation for the already-approved current-draft/policy loader. Existing 2B criterion: explicit customer submission plus trusted current verification and tenant policy creates exactly one job; this checkpoint supplies current inputs, NOT job creation. No scope deviation.

## Implemented

customer-intake-continuation.service.ts now supplies a request-local reader matching P03 readSubmission and a database-backed P02 current-state port. It reuses the version2 validator, customer credentials, normal session/history locks, encrypted organization-bound transcript, organization/payment parsers and LifeSafetyService. Exact request/session/tenant/conversation/credential ID and revision are required. Captured draft/components are copied; submission digest binds customer/internal data and approved policy versions, never provider content. Earlier customer-authored safety warnings and the current description use the existing emergency guidance and refuse before category/provider work; patterns were not expanded and a non-match is not a safety assessment.

Current active tenant and category rows are share-locked. ServiceCategory has no active flag; same-tenant existence plus exact name and configured allowed ID are checked. Missing/ambiguous categories refuse. Approved organization/payment values are parsed and hashed canonically; database time controls approval and credential freshness, and P02 checks revocation/expiry without a positive cache. No session token/history/provider response is returned as evidence. No new durable storage, schema, job write, DI/route registration or provider call. This reader is ready for P04 composition; it is not connected to a customer submit route yet.

## Validation

- 15 new cases in the existing continuation spec, including copied input/digest changes, wrong identities, stale history, category ambiguity, policy/capability changes, expiry and sticky safety refusal. Full backend: 116 suites passed, 1 skipped; 2,256 tests passed, 3 skipped.
- Backend lint, generated-client build, architecture and full/production dependency audits passed; zero audit findings.
- Existing disposable organization harness extended: real encrypted transcript and policy/category reads; concurrent identical reads; policy update fails specifically with lock timeout while reader holds its transaction; removing payment approval afterward refuses. Zero jobs/providers from this reader.
- Existing organization/customer/operator desktop/mobile browser regressions passed, including legacy admission replay/rollback. Those LEGACY write results do not prove the still-unimplemented controlled writer. Mobile draft screenshot visually inspected; no new UI was implemented. UI 170 tests, lint and static build passed. Existing framework/pg deprecation warnings remain nonblocking.
- Initial harness attempt failed because isolated UI dependencies/export were absent; installed locked UI dependencies without scripts, built the existing UI, then reran. The initial database fixture approval hash used raw JSONB key ordering; corrected the fixture to use the same approved-profile parsers as the application. No production workaround or guard weakened. Final full harness exited zero and cleaned its disposable database.
- Full governance consistency/frozen baseline and all 21 regressions, backend cross-repository gate and both whitespace checks passed at final closeout.

Reproduce: npm test -- --runInBand; npm run lint; npm run build; node scripts/architecture-check.mjs; npm audit --omit=dev; npm audit. Database/browser: build ui, set ORGANIZATION_EVIDENCE_DIR to a fresh /private/tmp directory and PLAYWRIGHT_MODULE to the installed local Playwright index.mjs, then node scripts/verify-organization-profile.mjs. Actual run output: /private/tmp/signmons-p04-reader-db.log; local evidence: /private/tmp/signmons-p04-reader-evidence.LHRt8v/summary.json and organization-intake-summary.json. Harness creates/applies migrations/drops only random calldesk_org_<hex> via local Unix socket, never a live DATABASE_URL.

## Remaining / review

Review the two continuation methods, their tests, and the added organization harness block. Next observable P04 result remains one controlled atomic job with version2 allowlisted audit/consent/link state and exact authenticated replay; race, audit failure, lost acknowledgment and final expiry must be proved on that writer. P05 route/UI composition and P06 explicitly approved live acceptance remain unchanged. No new package or dependency. P04 ACTIVE; no completed timing sample. Accepted walkthrough 3/8 (37.5%); accepted remaining-plan 2/60 (3.3%), neither is overall MVP completion. Original low-confidence 60–84 productive-day baseline unchanged pending completed-package samples.

Timing: first recorded clock in this resumption 15:12:40 UTC, validation closeout 15:20:35 UTC on 2026-09-13; entry inspection preceded the first clock and Git closeout follows it, so this is not a complete turn/package duration. No external owner wait. Keep P04 timing open rather than counting this interval as a completed sample. Rollback leaves composition absent or reverts this focused change. No merge/deploy/live sending, paid requests, provider/IAM/secrets changes or customer/appointment mutations.
