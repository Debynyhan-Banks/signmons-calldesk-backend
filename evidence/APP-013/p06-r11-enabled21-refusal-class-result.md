# P06/R11 enabled21 refusal-class diagnostic result

Owner-approved operation `8cba60f6-f4e8-40ed-b201-6d09e09eb90c` ran once during the authorized 6:00–6:45 AM Eastern window on 2026-09-23. Its read-only Cloud Logging query was limited to enabled21, the historical `2026-09-22T17:29:45Z`–`17:29:50Z` event interval and records containing `HTTP exception diagnostic`. It returned zero records and the helper reported `UNCONFIRMED` / `DIAGNOSTIC_COUNT` with count zero. The operation is consumed and was not retried.

Local source inspection explains the absence. `customerSessionHttp` is mounted before Nest guards and filters and explicitly owns its errors. `CustomerConsentBrowserTransport.handleBody` catches the controlled submission exception, converts it to an allowlisted status and returns the generic browser response. The global `SanitizedExceptionFilter` therefore never receives this HTTP 409. Although the transport supports an optional status-only `diagnostic` callback, `controlled-intake-runtime.ts` does not provide it, and the callback contract cannot distinguish the internal pre-address refusal stage. The query's assumed application record could not be emitted by this path.

The prior evidence remains valid: one submit returned HTTP 409 in 2.273183664 seconds, with zero committed jobs and zero address request/operation rows. The exact pre-address refusal branch remains unknown. Another log query or unchanged browser run cannot resolve it. The smallest useful next action is an owner-approved local-only repair that adds a fixed, privacy-safe internal refusal-stage enum to the controlled path, wires the existing diagnostic seam, and proves it with focused tests before any new packet or live action. P06 remains 12/14 with R11/full R12 open. No scope deviation implemented.
