# R02 private administrator handoff — local implementation

Owner said proceed after explicit local implementation/testing proposal. Entry backend7323a7b603a214de90b51b87c6362f27d0e2a231/governance126978f9a8c7889148a1149132e667a06d11c320, focused origins fetched clean; saved unrelated work preserved. Approved pre-code card completed in APP013_P06_R02_PRIVATE_ADMIN_PROCEDURE.md before runtime edits. No real administrator input, Neon connection, password change, encrypted-image mount, backup, provider request, migration or deployment this turn.

## Implemented within existing R02 area1

- p06_private_input.py extracts a non-echoing memory-returning TTY primitive. Existing runner capture API remains for regression tests; the CLI now requires --administrator and refuses former unqualified no-argument capture. Fixed Node child receives input through anonymous stdin pipe; minimal environment, no credential arguments/files, suppressed child error payloads, bounded cancellation and terminal restoration. Owner must launch in their private terminal, not an agent-captured PTY. Python/Node memory copies are not forensically erased.
- p06-private-role-password.mjs requires clean exact SHA, v2 packet and explicit administrator/inherited-credential/provider-log review fields before prompting. Fixed child TLS endpoint/user;5second connect timeout;120second handoff budget capped by remaining20minute window. Exclusive persistent handoff-attempt marker; no retry. Administrator password only in process memory, not runner passfile or backup process.
- Before generated secret SQL: exact identity, two existing NOLOGIN restricted roles/memberships, administrator CREATEROLE plus ADMIN OPTION, zero runner/other database sessions,26readable/nonwritable owned tables, logging settings/extensions fingerprint. No logging/configuration/permission changes to make checks pass. Missing visibility, unsafe settings, audit/utility logging or missing reviewed fingerprint refuse. Provider approval fields are operator attestations, not cryptographic proof or evidence of no provider logging.
- One generated256-bit runner secret to exclusive0600 scoped file, fsync before assignment. One fixed role PASSWORD/VALID UNTIL statement, preserving NOLOGIN; unchanged-role/security/catalog/expiry readback. Sent/commit/readback failures classified indeterminate with no automatic resubmission. Administrator connection closes before ASSIGNED_NOLOGIN receipt. Failures remove only same-inode runner file and attempt image ejection; cleanup failures stay failures. A refusal before owned attempt leaves operator-owned storage for explicit closeout.
- Success intentionally retains the mounted runner-only file for the next authorized step, not a fully closed backup window. No LOGIN granted. Existing backup now accepts fixedv2 only and requires matching handoff receipt/SHA/expiry/role/administrator-closeout. Existing backup cleanup and explicit Console runner-NOLOGIN/session closeout remain. Cancelledv1 records are not changed, adopted or deleted.

## Actual local validation

- Seven Python tests passed: original four runner-input regressions plus private anonymous-pipe success, cancellation/timeout/termination/early-child/error-payload cases, CLI/non-TTY refusal. Dummy canary remains absent from captured transcripts and child arguments/environment; no actual administrator credential used.
- Combined Node suite:28passed,1opt-in local-PG test skipped in normal run. Covers packet/v1/target/role/allowance/review refusal; connection/certificate-error redaction (mocked, not a real TLS handshake); logging refusal; exclusive markers/private files; one assignment/uncertain mutation; bounded pipe input; handoff receipt binding; existing20backup/guard tests including signal/time/storage/cleanup behavior.
- Separate actual PG18.6 proof passed the same assignCore on the existing private no-TCP cluster /private/tmp/signmons-pg18-YjyVW4. Dummy admin has CREATEROLE and ADMIN OPTION, dummy runner NOLOGIN/no elevated flags;26empty table names model the fixed allowlist without creating a second recovery rehearsal. One generated password assigned; NOLOGIN connection refused, test-only LOGIN allowed correct SCRAM password and rejected a wrong one, thenNOLOGIN. Catalog/role/expiry comparison passed. Exact dummy database and all three created roles removed; independent count readbacks0/0; server stopped. No real or historical customer rows used.
- First local proof correctly refused hidden logging settings: disposable administrator lacked pg_read_all_settings. Added that metadata-only grant to the dummy administrator to model Neon's documented pg_monitor inheritance; runner privileges unchanged. Local server logging settings were fixed at startup only for this generated fixture; no Neon setting inferred or changed. A dedicated local HBA file enforced SCRAM only for the dummy runner over the private socket. The corrected proof rerun passed; log/code paths are not evidence of Neon's actual effective settings.
- Lint/build/architecture, JS syntax/Prettier, frozen/full cross-repository consistency,21governance regressions and whitespace passed. No full application Jest or application browser result claimed: no app UI/route changed. TTY proof uses dummy pseudo-terminals. Full live adapter plus real Neon TLS/provider logging remains unexecuted.

## Next observable result / remaining limits

Review exact implementation and refreshed runbook. Next is READ-ONLY Neon logging/settings/extensions and administrator membership qualification, without retrieving passwords. Need a reviewed provider-logging record and matching fingerprint; current live values are unknown. If unsafe/unreadable, stop and report the specific requirement rather than weakening checks or adding another standalone rehearsal. Then obtain one fresh explicit live packet/SHA/window for private administrator handoff, LOGIN and backup/closeout. No blanket credential authority from this local implementation approval.

Original seven-day v1 record retention2026-09-22T12:22:00Z unchanged. No v2 directory/image mount or live packet created. Inherited administrator password is inherently broader than child scope; fixed target limits this helper, not the credential itself. No guarantee against host compromise, memory/swap capture or provider-internal logs. Local core and unit tests do not accept R02 or establish recoverability.

Approved credential-mechanism amendment implemented locally only; no additional scope deviation. R02 retains TWO acceptance areas, not a number of turns. R01closed,R02-R12open11,added0; accepted packages5/60(8.3%),walkthrough3/8(37.5%) unchanged; ETAunvalidated.
