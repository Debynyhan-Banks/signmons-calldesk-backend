# P06-R03 tested migration packet

## Three preparation items

1. Assembled: scripts/p06-migrate-once.mjs is a fixed-child, import-inert executor using the existing private Python pipe and Prisma7.10. All26SQL inputs unchanged from runtime source53037fbd118cc4547061dfaf373c45b20a05962b. p06-r03-proof.json lists all26SHA256 values; its last13 are the exact pending set. Proof-file SHA256:46f3ad11cd88cae0829c95d5743928f2b572227f62aaa98ef0b59fc471a07d5f. Old/new structural catalog fingerprints are generated from disposable PG18, not customer data. The R03 governance packet pins the final source commit.
2. Tested: existing upgrade rehearsal now calls the same runPrisma and migrateCore as the fixed adapter. Actual PG18 old13-to26 migration, no-op, constraints/FKs/uniqueness/immutability, catalog parity, guarded replay refusal and private-input regressions pass. A synthetic-only migration asserts5s lock_timeout and1min statement_timeout inside Prisma's DDL connection. Another synthetic-only pg_sleep(65) fails at the60second statement timeout; locked existing table fails at5seconds and leaves partial migration history, with no resolve or retry. Synthetic extra migrations are never in the reviewed repository manifest or live export.
3. Presented: governance APP013_P06_R03_PACKET.md contains target, exact runner/command, recovery, risk/maintenance, proposed window/allowance and review instructions. Owner execution approval still required; R03 gate remains open until that approval. R04 has not run.

## Safety and honest limits

Target is only child br-sparkling-sun-ay6gr5e8, direct ep-jolly-flower-ayc6w9hv.c-5.us-east-2.aws.neon.tech, neondb/public, neondb_owner. Parent access remains an acknowledged credential power, not least privilege. No new role/password/ownership transfer. No shared secret or old Cloud Run job used.

Runner checks clean pinned HEAD, pinned Prisma7.10, unchanged full SQL manifest and proof hash, exact owner/target/role, explicit migration approval and credential-risk acknowledgment, absolute<=10minute window,<=5minute resource/consumer observation, valid accepted archive/hash/expiry, exact encrypted mount/ownership/exclusion, exclusive private attempt reservation, schema/history/identity/ownership and absence of other sessions. Local input export is exclusive and checksum-verified. Database advisory lock plus private attempt marker refuses competing executions using this workflow; neither locks out arbitrary administrator clients. Exclusive child maintenance remains necessary; do not claim proof no external client can ever connect.

Raw Prisma stdout/stderr are discarded for live use, capped at1MiB before abort; fixed result only. Password travels non-echo TTY -> anonymous pipe -> transient driver/child environment, never shell arguments, committed env, password file or raw output. JavaScript/driver/OS memory is not guaranteed forensically erased; same-user/admin process access remains a risk. Node guard requires verified TLS; Prisma uses sslmode=require and sslaccept=strict with system trust. PostgreSQL/libpq verify-full is not a supported Prisma-native sslmode and could fall back to prefer, so it was corrected before any live use. Sources: https://www.prisma.io/docs/orm/v7/core-concepts/supported-databases/postgresql and https://github.com/prisma/prisma-engines/blob/main/quaint/src/connector/postgres/url.rs . Local socket rehearsal does not prove a live TLS handshake; failure must refuse without fallback.

Overall budget cancellation kills the Prisma process group and closes the guard connection; server statement timeout also bounds abandoned statements. Timeout/cancellation is NOT an atomic rollback of prior migrations. Fixed sanitized failure result means REFUSED_OR_PARTIAL; retain evidence, do not automatically retry, migrate resolve/reset or restore. The helper deliberately reports image closeout still required: R04 must verify processes/connections ended and detach the exact encrypted image before completion. No password file is created by this migration path.

Accepted recovery retained unchanged through2026-09-22T14:01:19.919Z. Runner rehashes existing198833byte archive and verifies result metadata; no re-export. Operator must confirm no invalidating source changes; matching schema/history does not independently prove absence of all row updates. R04 must reassess if source drift, consumer uncertainty or expiry invalidates recovery.

## Validation and review scope

Final exact-source PG18 report: /private/tmp/signmons-p06-upgrade-83Uj89/report.json. Statement timeout failed as expected after60520ms including CLI startup; lock timeout after5367ms. Four generated databases removed. Final proof equals the committed manifest/catalog artifact. Owned cluster /private/tmp/signmons-pg18-9NSAxT/data stopped; pg_ctl reports no server running. Synthetic files retained for review only. Combined focused Node tests41passed,1old opt-in role integration skipped;11Python tests passed. No encrypted real-data image opened or private credential requested during preparation.

Full application lint/build and2359Jest tests passed (3skipped;129suites passed,1skipped). Focused migration tests8passed; existing backup/private-role regressions remain passing with old opt-in role integration skipped. Python private-input tests11passed, including migration protocol success/failure/cancellation without canary output. PG18 suite uses private socket/no TCP; generated databases are removed and exact owned server stopped at closeout. No new UI behavior; product browser QA not applicable, and no fresh staging browser acceptance claimed. Governance frozen/full consistency,21regressions, architecture and whitespace must pass before push.

Review scripts/p06-migrate-once.mjs and its tests, shared rehearsal diff, private-input dispatch and proof manifest; then the single governance execution packet. No application routes, Prisma SQL/schema, provider configuration, secrets, deployment, backup or real data changed. P06 remains2/12qualification tasks closed, R03-R12open10, added0; accepted packages5/60(8.3%),walkthrough3/8(37.5%),ETAunvalidated. No scope deviation.
