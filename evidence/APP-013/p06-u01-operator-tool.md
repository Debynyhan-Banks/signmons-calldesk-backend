# P06-U01 — local operator-tool qualification

Owner explicitly approved U01 after R05 design review. Entry backend3b2ba06/governance48871f6; approved card APP013_P06_R05_DESIGN.md was updated before implementation. No app runtime/schema/dependency change. Original12 + approved-added1 =13 tasks. R01-R05closed; U01 now locally complete3/3 and review-ready; R06-R12 remain7. WholeP06 and customer walkthrough acceptance unchanged: packages5/60(8.3%),walkthrough3/8(37.5%); ETAunvalidated. No scope deviation beyond the expressly approved U01 addition.

## Three completed exits

1. scripts/p06-runtime-packet.mjs: prepareBundle copies/revalidates exact packet, requires current action-bound owner authorization record, reserves an operation before any secret access, validates distinct numeric purpose references/bytes, creates only the exact startup map and compares uploaded private readback before emitting a sanitized receipt. googleSecretPorts uses fixed authenticated REST endpoints, no automatic retries,10second request timeout; encryptedReservation creates a fresh exclusive operation directory in the existing verified encrypted image. Those real adapters were NOT invoked. Read-only gcloud project metadata confirmed signmons project number845074063310 for strict alias normalization; no secret payload access. Unknown upload outcome consumes reservation and requires reconciliation, not automatic retry.
2. operate supports activate/revoke/readback through a branded fixed-child TLS database handle or explicit local-only constructor. Exact26-file migration history, actual identity, ACTIVE tenant/current organization/payment/category checks, row lock/CAS, separate runtime/phone digests, last prewrite DB-clock check and audit commit together. No tenant activation or policy approval is inferred. Replay audit prevents reactivation with a consumed operation even after revocation; foreign/newer digest refuses cleanup. readback reports storedApprovalState separately from tenantStatus/windowCurrent, not a claim of usable runtime authority.
3. Eight new Node tests plus eight existing migration-helper tests pass (16total); actual disposable PG18 verifier passes eight grouped acceptance/failure checks using all26 real Prisma migrations and existing current-policy/startup services. Full application2359tests pass,3existing skips; build/lint/architecture pass. Governance frozen/full consistency and21regressions plus both whitespace gates apply at closeout. No UI change; no new browser-journey acceptance claimed.

Files: scripts/p06-runtime-packet.mjs; scripts/p06-runtime-packet.test.mjs; scripts/fixtures/p06-runtime-packet.mjs (fictional inputs only); scripts/verify-p06-runtime-packet.mjs; this evidence and boards/governance. No existing runtime file changed.

## Actual local evidence and limitations

Final database run: /private/tmp/signmons-u01-twlVEr. Old failed fixture run correctly refused an empty FAQ list; corrected fictional profile satisfies existing policy parser, no guard weakened. Final proof: invalid policy/category/inactive tenant refusal; checksum drift refusal; concurrent activation once/stale refusal; audit-correlated readback after a discarded-response scenario; foreign-digest revocation refusal; prepared bundle through actual startup/current readers with zero provider requests; revocation waits for reader transaction and new startup refuses; audit-trigger failure rolls back; expiry refuses activation while authorized revoke succeeds. Audit readback is demonstrated; an actual network-level lost-COMMIT-ACK injection was not performed. All created databases dropped and owned clusters stopped; only fictional local logs/input SQL remain. No recovery image mounted or live migration repeated.

No real Cloud Secret Manager round trip, IAM qualification, live TLS connection or deployment claimed. Google REST request/response shapes checked against official access/addVersion references on2026-09-15; API ports tested with fake responses only. https://docs.cloud.google.com/secret-manager/docs/reference/rest/v1/projects.secrets.versions/access and https://docs.cloud.google.com/secret-manager/docs/reference/rest/v1/projects.secrets/addVersion . Numeric source versions and bundle version still require R08 real readback. Bundle creates another copy of credentials: revoke DB authority before bundle retirement; existing instances can retain material. JS strings cannot be reliably zeroized; no dumps/logging and short-lived operator process remain required. Existing administrator retains broad access and must not become the app runtime role by inference. Operation authorization is an owner-reviewed record, not a cryptographic human identity proof.

## Reviewed interface and exact local review commands

Run from focused backend: npm run build; node --test scripts/p06-runtime-packet.test.mjs scripts/p06-migrate-once.test.mjs; node scripts/verify-p06-runtime-packet.mjs; npm run lint; npm test -- --runInBand; npm run arch:check. The verifier requires installed /opt/homebrew/opt/postgresql@18/bin, creates a fresh private Unix-socket cluster and never consumes DATABASE_URL or provider credentials. Do not point it at staging. Direct node scripts/p06-runtime-packet.mjs refuses without performing actions.

Future separately reviewed invocation uses exported interfaces, not a new endpoint:
- reviewPacket(nonsecretPacket) binds exact envelope/facts/source, fixed child, bundle resource and forbidden-purpose resources; use its runtimeDigest,phoneDigest and packetDigest, not handwritten hashes.
- prepareBundle(packet,authorization,googleSecretPorts(authenticatedGoogleClient,encryptedReservation(authorization))). Only after R07/R08 resource/spend/read authority. Auth client configured privately with existing ADC, never downloaded service-account keys. Review/verify clean source and built imports before invoking; sourceRevision is a bound provenance record, not self-attestation that the running checkout matches.
- fixedChildDatabase(privatePassword) takes existing private anonymous-pipe material, never argv/plain env or chat. operate(packet,authorization,handle,expected) accepts action activate/revoke/readback. Always close handle in finally. R07 must explicitly approve this privileged target/credential use; migration approval does not authorize it.
- activate expected is {updatedAt,approvals:{runtime,phone}} from approved readback; revoke expected is {runtimeDigest,phoneDigest}; readback expected is {operationId}. Authorization fields exactly owner,action,operationId,packetDigest,sourceRevision,startUtc,endUtc; <=15minutes. Readback/revoke can use a fresh cleanup window after run expiry; no extension of activation. Failed/uncertain calls emit only P06_PACKET_REFUSED_OR_UNCONFIRMED and never retry automatically.

R06 owns read-only current tenant/integration/category/policy, exception identity, provider restrictions/rates, liabilities, participant notice/eligibility, request caps and verification/infrastructure cost qualification. R07 reviews the exact release/configuration/invocation diff; R08/R10 still own provisioning/run gates. Preparation accepts no implied phone/address request, no new caller credential and no production action. Follow existing R06 next after review, not another U01 subsection. Active work time was not instrumented accurately enough for a timing sample; excluded rather than fabricated.
