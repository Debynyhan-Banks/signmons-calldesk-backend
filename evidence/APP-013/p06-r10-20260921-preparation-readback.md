# P06 / R10 September 21 preparation readback

Owner selected September 21, 2026, 5:30–6:00 AM Eastern and explicitly approved read-only provider/target refresh and fresh packet preparation. Execution remains separately gated. Backend HEAD is `ba12d10eba1f0e532ec9f2d447db1237b833abd9`; governance HEAD is `b55dd9fee69bf733aac63a2caed8e6dfe761197f`. The existing uncommitted Intelligence extensibility amendment is preserved.

Read-only Cloud Run inspection confirms normal traffic remains 100% on `signmons-calldesk-staging-app013bounds`, latest Ready is `signmons-calldesk-staging-app013p06disabled`, and the enabled tag is absent. The proposed `signmons-calldesk-staging-app013p06enabled8` revision is absent. Bundle version 1 and child database URL version 2 metadata are ENABLED; no payloads were accessed.

A preparation assertion expecting the service desired template's intake envelope to be disabled failed. Follow-up read-only inspection distinguishes desired template from Ready revision: the desired template still names the original `app013p06enabled` and contains its expired September 19 01:30–01:45 UTC envelope. The actual latest Ready disabled revision has `enabled:false`, the accepted immutable image and child database reference version 2. Six separate safety flags in the service template are false. No evidence of a new deployment or currently enabled intake is established by the stale desired template. Its handling must remain explicit in subsequent packet review; do not silently weaken a guard or change live configuration.

Safari initially displayed cached Twilio geographic permissions. Reload redirected to sign-in, so that cached display is not accepted as fresh provider evidence. Owner manual sign-in is required; no login submission, verification request, configuration Save or provider mutation was performed by the agent.

Preparation is incomplete. No new packet, helper, authorization, reservation, LOGIN, activation, deployment, paid verification, database connection or customer action was created/performed. Fresh provider evidence and remaining participant/current-state qualifications must be completed before the exact execution review. Do not silently extend the selected window or reuse a consumed operation.

Frozen-baseline and cross-repository documentation consistency checks passed before this evidence addition. P06 remains 11/14; R10/R11/R12 remain open. No scope deviation.

## Superseding preparation result after owner sign-in

At approximately 5:20 AM Eastern, the fresh signed-in Safari page showed one of one Verify services protected by Fraud Guard, United States SMS monitored for fraud, US voice disabled, and other listed countries disabled. The verified-caller page showed one recipient. The owner confirmed it is the same approved privately bound number. Existing private participant/account bindings match the prior packet without retrieving a secret or recomputing the phone HMAC. The recipient continuity conclusion combines current UI evidence with owner confirmation, not a new cryptographic comparison of the UI phone.

Prepared private packet `e40cac38-98db-4e3d-b931-63456e78bfce`, plan `f680291d-f4ae-4f42-8132-654f394b4a66`, under `/Volumes/Signmons-P06/r10-final-run-20260921-0530`. Packet digest `2a4eb4f53ef97fd53b5813f65d0783370f4bf504c9ed0e163c5ffbc68f35ced0`; runtime digest `8ea5b4a1d473f6a46068bdc17102741f6bad29004adee486bd30c7273e9bdb4e`; phone approval digest `402972986c2d9d52b6b60c637d2e344f41a8d30d992c2f8d8c3d0e50d0bb65b2`. These are packet/approval digests, not participant HMACs.

Proposed support window is September 21 5:30–6:00 AM Eastern, with one connected runtime 5:40–5:55 and mandatory closeout by 6:00 (09:30–10:00 UTC support, 09:40–09:55 runtime). Revision is `signmons-calldesk-staging-app013p06enabled8`; normal traffic must remain on `app013bounds`. Fresh operation IDs, existing image/application source and numeric resource versions are bound. Existing compiled file hashes and application-source equivalence passed; production packet and controller validators passed. The operator implementation is backend `ba12d10`, including the approved approval-pair snapshot repair. Any subsequently authorized helper must use `{approvals}` rather than the old pre-transaction timestamp shape.

No helper, owner execution authorization, action reservation or live database preflight exists. Current database identity, role, approvals, policy/category and cap state remain mandatory guarded checks before activation; historical CLOSED evidence is not a fresh database read. The expired desired Cloud template is disclosed above and is not copied as current runtime authority. One guarded activation/readback followed by zero-normal-traffic deployment/readback and one supervised R11 journey is the proposed execution, with no automatic retries and mandatory closeout on any failure.

Existing limits remain one phone START/up to five CHECK operations, no resend, USD0.50 phone-flow upper bound within USD1 account ceiling including the existing hold; address USD0.10 per attempt, two requests and USD0.20 session cap; browser one start, 60-request budgets and two in flight. Provider holds are not reset. These are governed operational allowances, not an invoice or hard billing cap. No payment/booking/dispatch or customer-facing confirmation authority is included.

Validation this preparation: 31 focused controller/packet tests passed; packet/controller review and build-hash checks passed. Documentation, baseline, architecture and whitespace checks passed. Only preparation/evidence files changed; no runtime source, original dirty checkout, provider setting, secret, database or deployment changed. Exact owner execution approval remains required. P06 stays 11/14 with R10/R11/R12 open; no scope deviation.
