# APP-013 Review Checkpoint - Communication Availability Boundaries

Date: 2026-09-08

## qs dependency remediation (2026-09-08, review-ready)

- Owner requested continued risk remediation. Fetched/reconciled backend `bcee134` and governance `1b2061b`; APP-013 remains sole Now. Selected one narrow dependency section from the already-approved risk plan, explicitly announced before edits; authorized CREATE orchestration remains the next functional section, not implemented or activated here. Existing focused branches only; unrelated saved-checkout changes preserved.
- Backend lockfile upgrades only the `node_modules/qs` entry from **6.15.3 to 6.16.0** (version, registry URL and integrity). No manifest/override, framework/provider SDK, UI dependency, runtime application source, API, payment or scheduling behavior change. All current consumer version ranges admit this release.
- Maintainer advisories identify 6.16.0 as patched for [hostile constructor/isBuffer serialization](https://github.com/ljharb/qs/security/advisories/GHSA-4mjr-xmp4-gh2g) and [bracket/comma array-limit bypass](https://github.com/ljharb/qs/security/advisories/GHSA-x5fp-wj9c-mxmx). Small synthetic inputs reproduced both old-library defects: a caught TypeError and four array elements accepted with a configured limit of three. No large allocation, worker-crash payload or real endpoint attack was used.
- Runtime dependency presence is verified through Nest/Express/body-parser and Twilio; superagent also resolves it for tests. Nest uses extended form parsing and preserves raw bodies. **A complete exploitable CallDesk input-to-sink path was not established**: presence is not proof of exploitability; comma parsing is not enabled in the observed Nest parser. This is removal of a known vulnerable component, not a claimed confirmed production exploit.
- Initial lockfile update/install left stale local package files despite npm tree output; a clean `npm ci --package-lock-only=false --ignore-scripts --no-audit --no-fund` corrected the installed tree. Direct package-version and consumer-resolution checks then confirmed actual 6.16.0 files. Explicit build regenerated Prisma afterward.

### Validation and audit delta

- **12 new tests** in `src/common/qs-dependency.spec.ts`: four consumer-resolution checks, two bounded hostile-constructor round-trips, plain/bracket comma limits, ordinary encoding, and loopback Nest parser compatibility for exact URL-encoded/JSON raw bytes and nested forms. The tests exercise installed code rather than relying on the lockfile version alone.
- Backend lint/build, **50 suites / 610 tests** passed (1 suite / 3 existing skips), architecture and Prisma validation passed. Disposable PostgreSQL suite passed all **15 existing migrations**, policy/lifecycle races, recovery/intent proofs and **11 existing actual process-crash cases**. Zero real provider calls; random local fixture database dropped and absence independently verified.
- Backend full audit changed from **5 high / 10 moderate (15 packages)** to **5 high / 9 moderate (14 packages)**; both qs advisories are absent. These are npm affected-package counts, not unique vulnerability counts. Current omit-dev audit remains **4 high / 8 moderate (12 packages)**, so remaining findings must not be dismissed as development-only. Objective snapshot: `evidence/APP-013/qs-audit-summary.json`.
- UI unchanged: lint, **30 tests / 6 suites**, type check and **14-page static build** passed. Desktop/390px customer/dispatch, technician, urgency-policy and notification browser regressions run against the rebuilt UI using temporary evidence copies; no new rendered surface or live acceptance claimed.
- Both critical-only audit commands pass with **0 critical**. UI full audit stays **10 high / 1 moderate**. Full audits remain nonzero/unaccepted; no security waiver. Existing package deprecation, pg concurrent-query and stale Browserslist warnings remain.

### Review and remaining work

1. Review the incremental diff after `bcee134` on [PR #21](https://github.com/Debynyhan-Banks/signmons-calldesk-backend/pull/21): the single lockfile entry, new dependency test and evidence. Verify no unrelated package versions, manifest or runtime application files changed.
2. In the focused backend run `npm ci --package-lock-only=false --ignore-scripts --no-audit --no-fund && npm run -s build && npm run -s lint && npm test -- --runInBand && npm run -s arch:check && npx prisma validate`, then `node scripts/verify-sms-enqueue-intents.mjs`. Run `npm ls qs --all` and `node -p 'require("qs/package.json").version'`; expect 6.16.0.
3. Run `npm audit --json` and `npm audit --omit=dev --json`; qs should be absent while remaining findings still fail. In UI run lint/test/build and the existing synthetic browser harnesses. In governance run `node scripts/docs-consistency-check.mjs`.
4. Stop review-ready. Next after review: bounded authorized CREATE orchestration with upstream guards and explicit recovery ownership, or another explicitly selected dependency remediation section. Calendar/unknown-send recovery, external-state races, reschedule/cancel reconciliation, remaining dependencies and acceptance stay open. No executor/worker activation.
- No schema/new migration, provider configuration/credentials/IAM/billing, charges, real customer/appointment data, external messages, staging/production operation, merge or deployment. The fix is pushed branch code only, not a deployed remediation.
- Planning estimates remain **APP-013 ~85%; governed CallDesk APP-006 through APP-016 ~81%**. This reduces security debt without adding feature completion; not release readiness.

## Earlier: Policy-update Calendar guards (2026-09-08, review-ready)

- Owner agreed to the remaining-risk recommendation and approved proceeding with item 1, one bounded policy-update section. Fetched/reconciled backend `a048d3e` and governance `7f58671`; canonical pointer/boards/handoffs/ticket/contracts/evidence still name APP-013 as sole Now. Existing focused branches only; original saved-checkout changes preserved.
- Urgency override now rejects unfinished Calendar work before same-value replay. It replaces the direct JSON snapshot write with tenant/job/deletion/observed-updatedAt/no-unfinished-operation compare-and-set, advances updatedAt monotonically, and creates its audit only after a successful claim in the same transaction. A competing policy edit cannot be overwritten using an older snapshot.
- Payment exception APPROVE/REVOKE now select only unfinished-operation existence and reject a hold before policy/entitlement work. Existing client expected version is retained; the mutation also matches current job status/no-unfinished-operation and explicitly advances updatedAt monotonically. Existing required-gate/manual-override/Growth entitlement, revocation, closed-job and verified-payment behavior remains intact. No payment status, Checkout, webhook, amount, fee, subscription, Stripe API/SDK or credential/configuration change.
- Existing operations escalation remains available during a Calendar hold; no provider escalation was executed in this run. Urgency reads/request shape are unchanged: concurrency control uses the server-observed version, not a new client-reviewed-version contract. This section protects these two job-policy writers, not all tenant policy, subscription or asynchronous payment state.
- Used the Stripe best-practices skill to preserve server-side payment authority and existing webhook/Checkout boundaries. No skill-driven provider action, key change or SDK upgrade was needed or authorized.

### Validation and objective evidence

- Backend lint/build, **49 suites / 598 tests** passed (1 suite / 3 existing skips), architecture and Prisma validation passed. Ten new unit cases cover hold-before-replay, approve/revoke holds, missing-selection fail-closed behavior, same-millisecond version advance, unrelated policy preservation, lost claims and continued operations-escalation availability.
- Disposable Unix-socket PostgreSQL suite passed **15 existing migrations** and **12 CREATE/RESCHEDULE/CANCEL × PENDING/UNCERTAIN/APPLIED/NEEDS_REVIEW combinations**. Both policy mutations reject a reservation committed between read and conditional update. Reverse ordering rejects stale journal reservations. Competing urgency/payment writes preserve the winning JSON and exactly one audit; real audit failures roll back both policy and version writes. Tenant/deletion and terminal lifecycle rules remain enforced; terminal approval/revocation preserves urgency. No real payment or provider operation.
- Prior lifecycle/consumer/intent/executor/reconciliation proofs and **11 actual process-crash cases** still pass. **Zero real provider calls**; random local test database dropped and absence independently verified.
- UI application code unchanged: lint, **30 tests / 6 suites**, type check and static build (**14 pages**) passed. New synthetic urgency browser harness passed desktop **1440px** / mobile **390px**, **8 mocked requests / 4 intentional conflict POSTs**, zero page errors/external requests/overflow. Both hold and newer-version errors display without a success notice; operations escalation remains enabled. New screenshots visually reviewed: `policy-hold-urgency-desktop.png` and `policy-hold-urgency-mobile.png`. This does not claim browser acceptance of the API-only payment exception path.
- Existing customer/dispatch hold, technician hold and notification regressions passed against the rebuilt UI using temporary evidence copies (14/14/92 requests), preserving previous committed screenshots.
- Critical audit gates pass with **0 critical**; full audits remain nonzero and unaccepted: backend **5 high / 10 moderate**, UI **10 high / 1 moderate**. Dependencies/lockfiles unchanged. Existing pg concurrent-query and stale Browserslist warnings remain.

### Remaining gates and next section

- CREATE executor/creator/journal/reconciler remain unregistered; live scheduling is still legacy/unjournaled. No orchestration/worker activation or global cross-system serialization claim. Event/payment/entitlement changes outside these two job-policy writes, post-preflight/provider races, bounded recovery ownership/retry/review, reschedule/cancel reconciliation and SENDING crash recovery remain activation/acceptance work.
- No schema/new migration, real customer/appointment data, external sends, provider/Stripe/Twilio/IAM/secrets/billing configuration, charges, staging/production operation, merge or deploy. Prior journal migration and retention policy remain separately approval-gated.
- Stop review-ready. Planning estimates: **APP-013 ~85%; governed CallDesk APP-006 through APP-016 ~81%**, not release readiness. Next after owner review: one bounded authorized CREATE scheduling-orchestration section preserving upstream tenant/auth/payment/availability checks, with explicit reader/worker ownership and no activation until the complete protocol is reviewed. Dependency exposure triage/remediation remains a separate bounded risk item; confirmed reachable security issues take priority. Do not advance tickets.

### Exact review steps

1. Review the incremental diff after `a048d3e` on [PR #21](https://github.com/Debynyhan-Banks/signmons-calldesk-backend/pull/21): `urgency-review.service.ts`, `payment-requests.service.ts`, their tests, `scripts/verify-policy-calendar-guards.mjs`, parent fixture wiring, browser harness and evidence. Confirm hold-before-replay, conditional snapshot write, monotonic version and unchanged payment rules.
2. In the focused backend run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`, then `node scripts/verify-sms-enqueue-intents.mjs`. Use only the fixture-owned random local database.
3. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/policy-calendar-guard-browser-qa.mjs`. Inspect both screenshots and verify conflicts do not produce success notices or disable operations escalation.
4. In focused governance run `node scripts/docs-consistency-check.mjs`; review the new policy contract and unchanged acceptance/release gates. Full audits are not clean; checkpoint approval is not a security waiver or release authorization.

## Earlier: Lifecycle and technician Calendar mutation guards (2026-09-08, review-ready)

- Owner reviewed the inactive one-shot CREATE checkpoint and said proceed. Fetched/reconciled backend `df9143a` and governance `abed80c`, canonical pointer, boards, handoffs, active ticket, contracts and evidence. APP-013 remains sole Now; only the current focused feature branches changed, preserving unrelated saved-checkout edits.
- Completion and all six technician actions reject any unfinished Calendar operation before mutation or idempotent replay. The existence-only relation does not expose Calendar IDs or metadata. Completion now claims the observed job version; technician writes retain the client expected version and add current status/no-unfinished-operation predicates. Both advance updatedAt monotonically, retaining transactional audit and departure-intent behavior.
- Technician list/detail add `calendarSyncPending`; held cancellations remain visible within the existing tenant/assignee/deletion/200-record boundaries. Held or terminal jobs return no available actions. The field UI labels the reservation provisional, says not to travel/start/change status, hides mutation buttons even for inconsistent stale action arrays, and discards old detail after a 409. Existing authorized customer contact visibility is unchanged.
- No payment/urgency policy semantics, public completion request shape, schema, provider adapter or module registration changed. The prior journal table is still a runtime prerequisite for these relation queries; deployment/migration remains separately approval-gated.

### Validation and objective evidence

- Backend lint/build, **49 suites / 588 tests** passed (1 suite / 3 existing skips), architecture check and Prisma validation passed. Eighteen new unit cases cover pending actions/replays, completion fallback, exact-version claims and held visibility.
- Disposable Unix-socket PostgreSQL fixture passed **15 existing migrations**, all **12 CREATE/RESCHEDULE/CANCEL × PENDING/UNCERTAIN/APPLIED/NEEDS_REVIEW combinations**, all six technician mutations, tenant/active-technician boundaries, unchanged held job/audit/intent state, terminal behavior and departure-intent regression. Deterministic reservation-between-read-and-update tests reject both completion and technician writes; opposite ordering rejects the stale journal reservation. A real audit insertion failure rolls back completion and its version. Prior **11 actual process-crash cases** and all intent/consumer/reconciliation/execution proofs still pass. **Zero real provider calls**; temporary fixture database dropped and absence independently checked.
- UI lint, **30 tests / 6 suites**, type checking and static build (**14 pages**) passed. New desktop **1440px** and mobile **390px** synthetic browser QA passed: **14 mocked requests**, two intentional stale-action 409 POSTs, zero mutation requests from initially held pages, zero external requests/page errors/overflow. Rebuilt screenshots visually reviewed: `calendar-hold-technician-desktop.png` and `calendar-hold-technician-mobile.png`. A CSS grouping issue found during review was corrected and the final build/browser run repeated.
- Customer/dispatch Calendar-hold and notification browser regressions run against the new static build using temporary evidence copies, preserving previous committed screenshots.
- Both critical audit gates pass with **0 critical**. Full audits remain nonzero/unaccepted: backend **5 high / 10 moderate**, UI **10 high / 1 moderate**. Dependencies/lockfiles unchanged; existing pg concurrent-query and stale Browserslist warnings persist. This is not a dependency remediation or ticket acceptance claim.

### Remaining gates and estimate

- Inactive CREATE executor/creator/journal/reconciler remain unregistered. Legacy live scheduling is still unjournaled; no recovery worker or production robustness claim. Urgency/payment-policy mutation coordination, post-preflight/provider races, bounded ownership/retry/review controls, reschedule/cancel reconciliation, SENDING crash recovery and remaining messaging/acceptance stay open.
- No external messages, real customers/appointments, IAM/secrets/provider/billing configuration, charges, new schema/migration, staging/production operation, merge or deployment. Prior journal migration, retention policy and any activation require separate approval.
- Stop review-ready. Planning estimates: **APP-013 ~83%; governed CallDesk APP-006 through APP-016 ~81%**, not release readiness. Next after owner review: scope the next authorized scheduling-orchestration section only after resolving outstanding policy-version coordination and reader/worker ownership; do not activate the executor alone or advance tickets.

### Exact review steps

1. Review the incremental backend diff after `df9143a` on [PR #21](https://github.com/Debynyhan-Banks/signmons-calldesk-backend/pull/21): both job services/tests, `scripts/verify-lifecycle-calendar-guards.mjs`, technician UI/API/helper changes and the new browser harness/screenshots. Verify guard-before-replay, version/no-unfinished predicates and unchanged financial policy.
2. In the focused backend run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`, then `node scripts/verify-sms-enqueue-intents.mjs`. This fixture creates/drops only its own random local PostgreSQL database.
3. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/technician-calendar-guard-browser-qa.mjs`. Inspect both screenshots and confirm hold/no actions, then synthetic settled-to-409 detail removal.
4. Run `node scripts/docs-consistency-check.mjs` in focused governance. Review updated lifecycle/technician contract and next-action gates. Full `npm audit` still fails at high/moderate severity; checkpoint review does not waive those findings or authorize release.

## Earlier: Inactive one-shot CREATE execution (2026-09-08, review-ready)

- Owner reviewed the consumer guard checkpoint and said proceed. Fetched/reconciled backend `3c159b9` and governance `abac6af`, canonical pointer/boards/handoffs/ticket/contracts/evidence; APP-013 remains sole Now. Existing focused branches only, original saved-checkout changes preserved.
- Added narrow `CalendarEventCreator`, unregistered `GoogleCalendarEventCreator`, and unregistered `CalendarCreateExecutionService`. This is one internal execution section for an already-authorized, persisted CREATE reservation, not a replacement public scheduling flow, activation flag or recovery worker.
- Exact tenant/operation lookup, CREATE-only action and future-window/stable-ID checks precede a transaction. PENDING -> UNCERTAIN is committed together with an exact tenant/job/claim-version/window/no-event/unassigned/no-technician-status job version advance. Journal claimedUpdatedAt advances with the job. UNCERTAIN here means an attempt may have begun, NOT proof that any request left the process. A lost transaction acknowledgment never triggers insertion.
- Only the winning PENDING caller may insert. Concurrent calls, restarts and UNCERTAIN/APPLIED records return pending without another POST or competing read-back; FINALIZED returns a historical receipt, ABORTED/NEEDS_REVIEW remain held. No rearm/reset/replacement ID exists. Fresh journal-version and job-version checks precede insertion, refusing observed newer review decisions/edits.
- The adapter makes at most one encoded-target POST with the persisted 32-hex event ID, tenant/job/operation private markers, saved absolute window/time zone, a fixed reference-only summary, opaque/default/private event and no default reminders. No customer name/phone/address, attendee, description, conference, management token or message content is included. HTTP timeout is eight seconds and redirects fail; auth acquisition is not covered by that HTTP timeout. All response statuses/bodies, including 2xx/409, are discarded; no raw error logging or automatic retry.
- Every completed insert call, including a thrown/unknown adapter result, proceeds to existing read-back reconciliation. Only its matching identity/window/version evidence may atomically finalize journal/job/intent/audit. Missing evidence stays review-held; transient read failure remains uncertain. No slot release, compensating Calendar write, immediate queue processing or provider message.
- Google request semantics were checked against [Events: insert](https://developers.google.com/workspace/calendar/api/v3/reference/events/insert). Caller-provided IDs and private properties are supported; `sendUpdates=none` suppresses guest update requests but is not a general guarantee against all provider email/calendar behavior. Google warns IDs are not an absolute distributed collision guarantee; the local attempt latch and exact ownership read-back are still required.

### Competing-entry-point audit: activation remains blocked

| Existing path | Observed current protection | Remaining work before activation |
| --- | --- | --- |
| `src/jobs/technician-workflow.service.ts:197` | Tenant/assignee/exact version, no unfinished-journal predicate | Hold technician mutation/replay consistently; executor currently refuses already-assigned/technician-state jobs |
| `src/jobs/job-lifecycle.service.ts:62` | Tenant/deletion/current status, no exact version or journal predicate | Prevent completion of provisional reservations and close read/write race |
| `src/jobs/urgency-review.service.ts:91` | Tenant-bound direct update of urgency/policy | Preserve policy consistency across pending execution; no change made in this section |
| `src/payments/payment-requests.service.ts:187` | Tenant/exact version for payment policy exception | Coordinate policy-version invalidation without changing payment semantics; no financial code/configuration changed |
| Existing scheduling/customer/dispatch paths | Prior consumer no-unfinished and version guards | Preserve signed-token, tenant, payment-policy and availability checks when wiring the executor; no direct caller bypass |

This is a source audit, not a claim that all other mutations or cross-system races are solved. A job/journal can still change after the final pre-insert read, or Calendar after read-back. Generic technician/lifecycle/policy paths and reader/writer ownership require review before any activation.

### Validation and limits

- Backend lint/build, **49 suites / 570 tests** passed (1 suite / 3 existing policy skips), architecture and Prisma validation passed. Added adapter/executor tests for all returned statuses, invalid input, tenant/action/replay controls, transaction ordering, lost claim/commit, newer journal/job state, thrown insert and read-back failure.
- Disposable local PostgreSQL suite passed **15 existing migrations**, concurrent single execution, one intent/audit on replay, unknown insert acknowledgment with positive read-back, absent evidence without rollback, newer user edit preservation and lost attempt-commit acknowledgment. **Two actual process exits** before/after a synthetic insert prove retained attempt state and no automatic reinsertion; separate read-back either holds or finalizes. Prior nine crash cases and consumer/intent proofs still pass (**11 total crash cases**). Zero real Calendar/SMS/provider calls; random Unix-socket fixture database dropped and absence independently verified.
- UI unchanged: lint, **29 tests / 6 suites**, static build (14 pages), desktop/390px Calendar-hold regression (**14 mocked requests, 6 POST**) and notification regression (**92 mocked requests, 14 POST**) passed with no page errors. Screenshots generated in a temporary regression directory; existing committed visual evidence was not overwritten. No new rendered surface or live acceptance is claimed.
- Critical audit commands pass with **0 critical**; full audits still report backend **5 high/10 moderate**, UI **10 high/1 moderate** and remain nonzero, unaccepted release risks. Dependencies/lockfiles unchanged. Existing pg concurrent-query warning remains. Initial formatter mismatch and a fixture invocation before its build were corrected before the final clean sequence.
- No schema/new migration, production registration, public API/worker, provider configuration, credentials/IAM, billing/charges, real customers/appointments, merge, deployment or staging/production DB action. Prior journal migration and retention policy remain approval-gated.
- A crash after latching but before actual POST intentionally strands the reservation for read-only review; no absence-based automatic recreate. UNCERTAIN/APPLIED execution does not itself run recovery. Worker/review ownership and bounded recovery remain separate work. Existing live scheduling still uses the legacy unjournaled path; pre-finalization/unknown-outcome and SENDING crash risks are not closed by inactive code.
- Stop review-ready. Planning estimates **APP-013 ~81%; governed APP-006 through APP-016 ~80%**, not release readiness. Next after review: one bounded competing lifecycle/technician mutation-guard section, preserving payment policy; then complete authorized scheduling orchestration and bounded reader/worker ownership/retry/review controls. No activation until those gates are reviewed. Reschedule/cancel reconciliation, dependencies and remaining messaging/acceptance remain open.

### Exact review steps

1. Review incremental changes after `3c159b9` on PR #21, especially `calendar-create-execution.service.ts`, `google-calendar-event-creator.ts` and `scripts/verify-calendar-create-execution.mjs`. Confirm the persisted attempt latch precedes POST, retries never insert, read-back alone finalizes, and no module/controller/worker imports activate these classes.
2. In focused backend run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`, then `node scripts/verify-sms-enqueue-intents.mjs`. Use only the existing random disposable local PostgreSQL fixture.
3. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`. Run both `scripts/calendar-guard-browser-qa.mjs` and `scripts/notification-browser-qa.mjs` with `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs`; they intercept synthetic API requests only.
4. In focused governance run `node scripts/docs-consistency-check.mjs`; review the entry-point audit, contracts and next bounded section. Approval of this checkpoint is not merge/migration/deployment/provider activation approval.

## Earlier: Unfinished Calendar consumer guards (2026-09-08, review-ready)

- Owner reviewed the inactive CREATE read-back checkpoint and said proceed. Fetched and reconciled backend `f30760a` / governance `b2dcec5`, canonical APP-013 Now, boards, handoffs, ticket, contracts and evidence. Continued existing focused branches; preserved original saved-checkout changes. Exactly one section: customer, dispatch and notification consumers of unfinished Calendar operations.
- Shared existence-only relation selection uses `finishedAt: null` (PENDING, UNCERTAIN, APPLIED and NEEDS_REVIEW). An omitted relation fails closed. No Calendar ID, operation ID, provider payload or credential is exposed by the guard. FINALIZED/ABORTED history does not itself impose a hold; ordinary lifecycle/version checks still apply.
- Initial confirmation and every signed management action, including view and payment-link recovery, return HTTP 409 with an office-review message before exposing provisional details or invoking actions. Customer UI clears an old booking card on action/payment conflict. This is a fail-closed response, not a new success-shaped booking state or automatic repair.
- Changed customer confirmation/reschedule-request audits now share a transaction with an exact tenant/job/version/no-unfinished-operation conditional update and monotonic version advance. Legacy initial reservation, reschedule/cancellation claims, compensation and finalizers add no-unfinished predicates. Advancing the job version can conservatively invalidate an earlier reschedule replay receipt.
- Dispatch list/detail expose only additive `calendarSyncPending`; unfinished jobs use ESCALATED (including locally CANCELLED and already-assigned jobs), never READY_TO_ASSIGN. List remains tenant-bound, undeleted and capped at 200. Candidates are ineligible with fixed CALENDAR_SYNC_PENDING reason; assignment/reassignment/cancellation and same-technician/no-assignee replay cannot bypass the hold. Mutation predicates close the stale-read claim window. UI labels the reservation provisional and disables assignment controls even with an override reason.
- Transactional admission/intent capture/owner retry fail closed. Lifecycle recovery distinguishes CALENDAR_PENDING from stale state before digest comparison: enqueue intents retain PENDING and their failure count, with a 60-second delay and fixed `calendar_sync_pending` code. Delivery detects the hold before decrypt/consent/provider access, conditionally releases its SENDING claim back to QUEUED, refunds only that unsent attempt and delays 60 seconds. Direct delivery claims now honor due time. Lost claim ownership is a conflict, not a claimed successful release. Existing lifecycle hashes, deduplication, consent and terminal/unknown-send policies are unchanged; no automatic replay of possibly sent work.
- Files: shared guard/unit suite; scheduling and legacy finalizer guards/tests; dispatch service/tests/UI; message state/admission/intent/delivery/recovery tests; customer conflict UI and notification labels; disposable guard proof and browser harness/four screenshots; evidence/board/handoff and matching governance pointer/board/handoff/contracts/ticket/matrix. No package/lockfile, schema, migration or module-registration changes.

### Validation

- Backend lint/build, **47 suites / 535 tests** passed (1 suite / 3 existing policy skips), architecture and Prisma validation passed. New tests cover all template holds, missing-selection fail-closed behavior, customer actions/replay, stale customer audit claim, dispatch override/no-op refusal, enqueue retry preservation, owner retry refusal and unsent delivery claim ownership.
- `node scripts/verify-sms-enqueue-intents.mjs` passed **15 existing migrations** only in a random disposable Unix-socket PostgreSQL database. New guard proof exercises **12 real action/status combinations**, all seven customer actions plus initial confirmation, tenant-scoped pending cancellation visibility, current-version legacy claim rejection, admission/intent holds, fourth-failure enqueue preservation, last-attempt delivery preservation and immediate retry refusal. Terminal history removes only the hold: stale digest still stops; compatible state can enqueue again. Earlier atomicity/concurrency/reconciliation and nine process-crash cases remain green. **Zero provider calls**; fixture dropped and absence independently verified. Existing pg concurrent-query deprecation warning remains.
- UI lint, **29 tests / 6 suites**, static build (14 pages) passed. New Chrome QA at 1440px/390px passed **14 mocked requests (8 GET / 6 customer POST)**: pending customer view, stale-card clearing after conflict, visible dispatch hold, no recommendation, disabled assignment/cancellation despite reason, no overflow/runtime errors, and **zero dispatch mutation requests**. Four screenshots visually reviewed: `calendar-hold-customer-desktop.png`, `calendar-hold-customer-mobile.png`, `calendar-hold-dispatch-desktop.png`, `calendar-hold-dispatch-mobile.png`. Existing notification regression passed **92 mocked requests / 14 POST** with screenshots written outside the repository; prior evidence not overwritten.
- Initial missing-relation test doubles, one incorrect test method name, a self-referential mock typing issue, delivery claim field/placement errors and browser locator ambiguity were caught and fixed before the clean runs. Screenshot review caught a grid-layout conflict; the hold notice now sits in its own warning panel.
- Audit findings unchanged: backend **5 high / 10 moderate**, UI **10 high / 1 moderate**, **0 critical**. Full audits remain nonzero and are not risk acceptance. No dependency upgrade in this section.

### Boundaries and next review

- This guards recorded unfinished work; it does not discover legacy crashes without journals. Journal writers/readers/reconcilers remain unregistered and no recovery worker is activated. Existing Calendar write/unknown-outcome and pre-finalization crash gaps remain open until guarded integration and recovery are reviewed.
- Reads/admission/provider checks are snapshots, not a distributed lock. The interval after the delivery check and before provider send remains non-atomic. A crash after taking SENDING but before releasing a hold can still strand delivery; no unsafe automatic SENDING reset is added. Technician/job APIs outside this consumer section are not globally serialized by it; integration must audit those entry points before activation.
- These consumer queries require the prior journal-table migration at runtime. Any staging/production migration, retention policy, provider/configuration/live acceptance, merge or deployment needs separate approval. No real customer/appointment data, Calendar/SMS call, secrets/IAM/billing or release action occurred.
- **APP-013 remains Now, approximately 79%; governed APP-006 through APP-016 approximately 80%**, planning estimates, not release readiness. After owner review, separately scope guarded CREATE writer integration (saved event ID/operation marker and competing-entry-point safety), followed by bounded worker ownership/retry/review controls. Reschedule/cancel reconciliation, dependency remediation and remaining messaging/acceptance work stay open. Stop at this checkpoint.

### Exact review steps

1. Review the incremental change after `f30760a` on backend PR #21; inspect `calendar-operation-guard.ts`, customer/dispatch conditional writes, and the distinct enqueue/delivery hold branches. Confirm no journal/reconciler registration or external activation.
2. In the focused backend run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`, then `node scripts/verify-sms-enqueue-intents.mjs`. The fixture creates/drops only its own random local database.
3. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`; then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/calendar-guard-browser-qa.mjs`. Inspect the four new screenshots. Use only synthetic credentials/data.
4. In focused governance run `node scripts/docs-consistency-check.mjs`. Review the updated contract and unchanged APP-013/release gates; approve this section separately from any integration or release.

## Earlier: CREATE Calendar read-back reconciliation (2026-09-08, inactive)

- Owner reviewed journal foundation and said proceed. Fetched both remotes, reconciled canonical APP-013 Now, boards, latest handoffs/ticket/contracts/evidence and backend `cbdddc9` / governance `c0f47d8`. Continued existing focused branches; original saved-checkout changes preserved. Exactly one bounded section: connect persisted CREATE journal evidence to read-back validation and atomic local finalization, without activating scheduling.
- Added narrow `CalendarEventReader` boundary and `GoogleCalendarEventReader`. The adapter uses a single encoded saved-target GET, read-only Calendar scope, redirect refusal and eight-second HTTP timeout. It returns only allowlisted event identity/version/window/ownership fields. 404/410 are unverified (never permission to insert or roll back); auth/HTTP/rate-limit/timeout/JSON errors are unavailable with no raw error, payload or credential leakage. No Calendar write method exists.
- `CalendarCreateReconciliationService` supports CREATE only. Tenant-bound journal lookup and matching undeleted ACCEPTED reservation/version/window/label precede the read. Reconciliation requires the exact saved event ID, tenant ID, job ID and `signmonsCalendarOperationId` private marker, confirmed state, a bounded nonempty ETag and matching absolute start/end instants. All-day, recurring, nonblocking and nonstandard events are rejected. Past/already-started arrival windows require office review; time is checked again inside finalization.
- Matching evidence atomically transitions journal to FINALIZED, stores the job's Calendar reference and advances its version, captures the existing canonical confirmation intent, and writes one `appointment.initial_confirmed` system audit. Audit actor is SYSTEM_AI/`calendar-reconciliation`, not a fabricated customer action; metadata contains journal/intent IDs, fixed evidence code and SHA-256 ETag hash, not raw ETag or Calendar content. No queue processing, operations notifier or SMS send occurs here.
- Missing/conflicting evidence and changed local reservations stay NEEDS_REVIEW, with no automatic reopening/recreate/rollback. Transient read/persistence failures stay UNCERTAIN/pending and can be re-read later. Conditional original journal status/version prevents late responses overwriting a newer review decision or completed receipt. A committed receipt after acknowledgment loss returns `already_finalized` without duplicating intent/audit; this is historical completion evidence, not a new assertion of current Calendar state.

### Validation and remaining boundaries

- Backend lint/build, **46 suites / 510 tests** passed (1 suite / 3 existing policy skips), including **46 new tests**; architecture, Prisma, formatting and diff checks passed. Reader tests cover request method/encoding, field allowlisting, 404/410, 401/403/429/5xx, unavailable auth, malformed JSON, transport failure and incompatible event shapes. Reconciliation tests cover identity/window/version/tenant/time/replay/atomicity controls.
- `node scripts/verify-sms-enqueue-intents.mjs` passed all **15 existing migrations** in a disposable local Unix-socket PostgreSQL database. New `verify-calendar-create-reconciliation.mjs` uses the real journal/reconciler/Prisma/intent path with a synthetic Calendar reader. It proves four-record atomic finalization, actual rollback after job/intent/audit writes, recovery on a later valid read, one concurrent finalization, acknowledgment-loss receipt recognition, late-failure versus completed receipt, late-valid-read versus newer review hold, and preservation of a newer job edit.
- Three new child processes exit after synthetic Calendar read, after transaction writes before commit, and after acknowledged commit before return. The first two leave no success audit/intent/reference and recover from the persisted journal; the third preserves exactly one finalized journal/audit/intent on replay. Prior six journal reservation crash cases and all appointment/messaging proofs remain green. **Zero external Calendar/provider calls and zero queue creation by reconciliation**; disabled processing leaves its intent pending. Fixture database dropped; independent pg_database query verified no matching fixture remained. Existing pg concurrent-query deprecation warning remains.
- A test-double callback typing issue initially failed lint. An early database run used stale compiled output and caught the old pending-versus-needs-review behavior. The typed double was fixed and the complete lint/test/build/architecture/Prisma/database sequence rerun successfully against final source; neither failure is concealed as a pass.
- Unchanged UI lint, **6 suites / 29 tests**, and static production build (14 pages) passed. Existing synthetic Chrome desktop/390px QA passed **92 mocked requests (78 GET/14 POST)** with no page errors; all four screenshots were visually inspected. The unchanged harness was copied under `/private/tmp/signmons-create-readback-qa.rv5tlV/ui/scripts/` and linked to this checkout's `ui/out`; generated screenshots live in that temporary directory's `evidence/APP-013/`. Previous committed images are unchanged. Playwright module: `/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs`. Browser/server closed; fixture regression, not new UI/live acceptance.
- Fresh full audits remain backend **5 high/10 moderate**, UI **10 high/1 moderate**, **0 critical**. No dependency/lockfile changes or risk acceptance. Governance consistency passed; all acceptance boxes remain unchecked.
- IMPORTANT: journal, reader and reconciler remain absent from SchedulingModule/routes/workers. Existing live booking still does not persist the required operation marker/ID. This code cannot adopt legacy Calendar events or discover absent journals. No CREATE writer, retry worker/backoff/lease policy, review UI, reschedule/cancel reconciliation, consumer pending-state guards or historical backfill is activated. A read is only a point-in-time observation: a later manual Calendar edit can still diverge, and Calendar/PostgreSQL are not atomic.
- Next bounded section after review: journal-aware unfinished-operation guards for customer views/actions, dispatch eligibility and notification admission/delivery. Then reviewed stable-ID/operation-marker CREATE integration and bounded recovery scheduling; never activate the journal alone. Prior migration/retention policy, provider configuration, real/live acceptance and release remain separately approval-gated. Remaining dependency remediation, reschedule/cancel recovery, events/templates/preferences/email and acceptance remain open.
- No new migration/schema, staging/production database, real customer/appointment, provider configuration, IAM/secrets/billing, live message, merge or deployment action. Planning estimate: **APP-013 approximately 77%; governed APP-006 through APP-016 approximately 80%**, not acceptance or release readiness. Stop review-ready.

### Exact review steps for CREATE read-back

1. Review the incremental commit after `cbdddc9` on PR #21: `src/scheduling/calendar-event-reader.ts`, `google-calendar-event-reader.ts`, `calendar-create-reconciliation.service.ts`, their two unit suites, and `scripts/verify-calendar-create-reconciliation.mjs` plus parent fixture wiring.
2. From the focused backend checkout run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && node scripts/verify-sms-enqueue-intents.mjs`. The fixture uses only /tmp:5432 and its own random database; build before running it. Do not target staging/production.
3. In `ui`, run `npm run -s lint && npm test && npm run -s build`; optional browser rerun uses the existing synthetic harness with Playwright and Chrome, never real tokens/appointments.
4. Run full `npm audit` in backend and UI; current high/moderate findings mean nonzero exits and remain release risks. In the focused governance checkout run `node scripts/docs-consistency-check.mjs`.
5. Verify no production consumer/module registration and no Calendar write/queue call were added. Review mismatch/past-window holds, hashed ETag audit, atomic rollback and concurrency/ack-loss proofs before approving consumer guards. No deployment or live repair follows automatically.

Reference semantics were verified against the official [Calendar event lookup](https://developers.google.com/workspace/calendar/api/v3/reference/events/get), [event resource](https://developers.google.com/workspace/calendar/api/v3/reference/events), and [error handling](https://developers.google.com/workspace/calendar/api/guides/errors) documentation; no Calendar API call was made during this run.

## Earlier: Calendar operation journal foundation (2026-09-08)

- Owner explicitly approved the reliability/security recommendation and requested robust continuation. Fetched both remotes and reconciled APP-013 as sole Now, the canonical pointer, boards, handoffs, active ticket, contracts, evidence and focused commits. Started at backend `f7d5e1e` / governance `82fc1fb`; original saved-checkout changes remain untouched.
- Completed one bounded persistence foundation: `CalendarOperationJournalService.reserve` atomically conditionally claims a local CREATE/RESCHEDULE/CANCEL reservation and inserts its PENDING journal record. The exact tenant/job version, undeleted compatible lifecycle and observed calendar/window must match. Claim version advances monotonically even within one millisecond. Journal failure rolls the reservation back.
- Journal captures trusted server Calendar ID/time zone, a once-generated 32-hex create event ID (or the existing event ID), original lifecycle/window/label and desired window/label. Cancellation preserves the external deletion target even though its local reservation clears that reference. Scheduling text and Calendar identifiers stay private server-side; no message body, customer-management credential or provider credential is copied. No public read/write endpoint, success audit, notification intent or provider call is added.
- PostgreSQL enforces tenant/job composite foreign keys, ordered windows/versions, action snapshot shape, terminal-state/time consistency and one unfinished operation per job using a partial unique index. PENDING, UNCERTAIN, APPLIED and NEEDS_REVIEW all retain that lock. Existing scheduling-window uniqueness also remains authoritative. This serializes journal reservations only, NOT the existing live scheduling path or independent external calls.
- Additive migration `20260908180000_add_calendar_operation_journal` was applied only in the disposable local fixture. Journal rows restrict hard deletion of their job/tenant to avoid silently losing recovery evidence; archival/deletion policy must be reviewed before activation. No backfill, terminal transition API, worker lease/retry, Calendar read/ETag reconciliation, journal-aware finalization or live integration exists yet. The service is deliberately absent from SchedulingModule and all production consumers.

### Validation and remaining boundaries

- Backend lint/build, **44 suites / 464 tests** passed (1 suite / 3 existing policy skips), including 21 new unit tests; architecture and Prisma validation passed. Same-window reschedule requests are rejected instead of creating a new operation; the eventual caller must use finalized replay proof. One initial full-test process exited 139 during concurrent validation; two subsequent full runs passed, including the final changed source. Root cause of that process exit was not established; it was not an application crash-recovery test.
- `node scripts/verify-sms-enqueue-intents.mjs` passed all **15 migrations** in its random local Unix-socket PostgreSQL database. New proof runs six child processes: CREATE, RESCHEDULE and CANCEL each exit abruptly just before commit and just after acknowledged commit. Before-commit exit preserves the exact original job with zero journal rows; after-commit exit leaves the PENDING journal, original target/snapshot and matching claimed job version available to a fresh process. No Calendar operation is attempted in these crash cases.
- Real database proof also covers failure after actual journal insertion, one concurrent reschedule/cancel claim, direct partial-index rejection in all unfinished states, stale/deleted/cross-tenant rejection, composite FK/hard-delete protection, SQL CHECK constraints and no new SMS intent/message/success audit. All prior appointment finalization/notification recovery proofs pass. **Zero provider calls**. The fixture database was dropped and an independent pg_database query found no remaining matching database. Existing pg concurrent-query deprecation warning remains.
- Unchanged UI lint, **6 suites / 29 tests**, and production static build (14 pages) passed. Existing synthetic Chrome desktop/390px browser regression passed **92 mocked requests (78 GET/14 POST)**, including access/filter/duplicate-click/unknown-outcome/timeout/late-response/privacy controls, with no page errors. All four screenshots were visually inspected. The unchanged harness was copied to `/private/tmp/signmons-calendar-journal-qa.ZyLQhs/ui/scripts/` with `ui/out` symlinked to this checkout's static build, keeping generated images in that temporary directory's `evidence/APP-013/` rather than rewriting prior screenshots. Playwright module: `/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs`; Chrome default path from the harness. Browser/server closed. There is no new journal UI or live acceptance.
- Fresh full audits remain **backend 5 high/10 moderate; UI 10 high/1 moderate; 0 critical**. Dependency/lockfiles are unchanged. These are open release risks, not accepted findings or a clean audit; dependency remediation is a separately reviewed follow-up. Ticket acceptance remains unchecked.
- The live pre-finalization/unknown-Calendar-outcome gaps remain open because this foundation is not wired into scheduling. Next approved direction is a bounded journal-aware integration/reconciliation section, preserving auth/payment/signed-slot/availability controls and adding read-before-retry, conditional external writes, durable worker ownership, atomic finalization and office-review behavior. Customer views, dispatch eligibility and notification admission must respect unfinished journal state before activation. Do not activate the journal alone or let a local pending claim imply Calendar success.
- No staging/production database, real customer/appointment, provider configuration, secrets/IAM/billing, live message, merge or deployment action. Migration and activation require separate release approval. Planning estimates: **APP-013 approximately 75%; governed APP-006 through APP-016 approximately 80%**, not acceptance scores. Stop review-ready.

### Exact review steps for the journal foundation

1. Review the incremental commit after `f7d5e1e` on backend PR #21. Inspect the journal service, schema/migration, unit suite and `scripts/verify-calendar-operation-journal.mjs`. Verify SchedulingModule and production scheduling consumers are unchanged.
2. In the focused backend checkout run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate`.
3. Run `node scripts/verify-sms-enqueue-intents.mjs` with local PostgreSQL at Unix socket /tmp:5432 and current OS user. It refuses non-fixture database names in child processes and does not read deployment credentials. Expect 15 migrations, the six process-exit checks and zero provider calls; it drops only its own disposable database. Do NOT apply the migration to staging/production.
4. In `ui`, run `npm run -s lint && npm test && npm run -s build`. For optional synthetic browser regression, use the existing `scripts/notification-browser-qa.mjs` with an installed Playwright module and Chrome; never use a real operator token or appointment.
5. Run full `npm audit` separately in backend and UI; expect nonzero exit until the documented high/moderate findings are remediated or explicitly accepted. In the focused governance checkout run `node scripts/docs-consistency-check.mjs`.
6. Review the journal retention restriction and inactive integration boundary. Approve the next bounded integration section, not automatic production activation or ticket completion.

## Earlier: post-Calendar reschedule intent finalization checkpoint (2026-09-08)

- Owner reviewed cancellation finalization and said proceed. Fetched both remotes and reconciled canonical APP-013 Now, focused boards/handoffs/ticket/contracts/evidence and latest commits. Resumed backend `df97aa83b6615b94e462f91deae3492a064aa63a` / governance `50c3db7ba6a212f0dd2e8c34a9a3ca11ec1ff439`; current focused branches only, no ticket transition. Original saved-checkout edits preserved.
- Added `AppointmentReschedulingService` for the local reservation/finalization boundary. Claim matches original tenant/job/undeleted ACCEPTED state, calendar/window and exact version; it stores the new reservation without notification intent or customer-rescheduled audit. After Calendar acknowledges PATCH, finalization conditionally matches the claimed version/window and atomically advances the version, captures canonical `APPOINTMENT_RESCHEDULED` intent and records `appointment.customer_rescheduled` CUSTOMER audit. Existing previous/new appointment labels remain; intent ID and `finalizedUpdatedAt` are additive metadata. No message/phone/provider credential is logged or audited.
- Claim, finalization and compensation explicitly advance versions even within one clock millisecond. Same-window replay requires an audit for the tenant/job and exact current `updatedAt`, not just matching local dates. Pending/legacy/changed snapshots fail closed to office review; finalized replay causes no new Calendar request, intent or audit. An unrelated subsequent job edit can conservatively invalidate replay proof. No backfill or automatic repair is added.
- Failed finalization after acknowledged Calendar change keeps the new local reservation and does not PATCH Calendar back or report the original time as guaranteed. Intent/audit insertion failures roll back both writes. Successful finalization remains successful despite immediate worker, operations-notification or logging failure; durable worker recovery handles missed post-commit processing. Removed SchedulingService's obsolete direct transactional-messaging dependency/helper.
- Reschedule joins initial confirmation, cancellation and technician-departure intent processing/reviewed owner/admin retry. Existing default-disabled delivery, fixed templates, current-state digest, canonical queue identity, consent, quiet hours and five-failure limit remain. Changed window stops before enqueue. Notification-center eligibility/copy/fixture now cover all four current templates, not arbitrary messages.
- Files: new rescheduling service and unit suite; scheduling orchestration/module/tests; intent recording/shared policy/tests; new disposable reschedule fixture and parent/constructor fixture wiring; UI intent helper/copy/browser fixture/four screenshots; backend evidence/board/handoff and matching governance contracts/ticket/pointer/board/handoff. No schema/migration or package/lockfile changes.

### Validation and remaining boundaries

- Backend lint/build, **43 suites / 443 tests** passed (1 suite / 3 existing policy skips), architecture check and Prisma validation passed. Tests cover conditional/version-bound claims, same-millisecond version advance, atomic intent/audit ordering, replay proof, failures at each transaction stage, late compensation and notification isolation. A misplaced replay-check edit and initial formatting failures were caught and corrected before the clean run; initial-confirmation regression remains green.
- `node scripts/verify-sms-enqueue-intents.mjs` passed all **14 existing migrations** only in its random disposable local PostgreSQL database, including new `verify-appointment-rescheduling.mjs`. Real scheduling/rescheduling/Prisma/intent/recovery code with Calendar/operations/delivery doubles proved no intent before Calendar acknowledgment, audit/intent atomicity, pending/changed replay rejection, successful replay without another PATCH, disabled processing, worker deduplication, rollback after actual intent/audit inserts without Calendar/local-window rollback, one concurrent finalization, tenant/version guards, late compensation refusal, reviewed retry and changed-window stale rejection, and no intent on Calendar error. Prior confirmation/cancellation/technician/retry proofs remain green. **Zero provider calls**; fixture dropped and absence independently verified in `pg_database`. Existing pg concurrency warning remains.
- UI lint, **6 suites / 29 tests**, production static build (14 pages) and synthetic Chrome desktop/390px QA passed: **92 mocked API requests, 78 GET/14 POST**. The failed reschedule intent appears and requires verified role, reason and acknowledgment for review. All existing filtering, independent failures, duplicate-click, rejection/unknown/actual-timeout, late responses, credential-memory, private-field omission and keyboard checks pass; no page runtime errors. Four screenshots visually inspected: `reschedule-intents-desktop.png`, `reschedule-intents-mobile.png`, `reschedule-retry-desktop.png`, `reschedule-retry-mobile.png`. Browser/server closed; this is fixture evidence, not live acceptance.
- Prettier, diff and governance consistency checks passed. Critical audit gates pass, **0 critical**, with unresolved backend **5 high/10 moderate** and UI **10 high/1 moderate** findings. Lockfiles/dependencies unchanged; release triage/risk acceptance still required, not a clean audit.
- Limitations: local reservation still precedes external PATCH; Calendar/database are not atomic and external operations are not globally serialized. Process loss before finalization and unknown Calendar results require manual reconciliation. Retained pre-acknowledgment compensation is exact-version-bound, but cannot prove the external result; error copy no longer guarantees the original time. Customer view still reflects local state, not independently verified Calendar completion. Notification center cannot detect/repair absent intents. No global recovery/backfill or end-to-end scheduling durability claim.
- No provider/configuration, secrets/IAM/billing, live sends, real appointments/customers, staging/production DB, merge or deployment action. Prior intent migration remains release-gated. Planning estimates **APP-013 74%; governed APP-006 through APP-016 80%**, not acceptance scores. Remaining calendar reconciliation, events/templates/preferences/technician notifications/email, dependency triage and owner/live acceptance. Stop review-ready; APP-013 stays Now.

### Exact review steps for reschedule finalization

1. Review the incremental commit after `df97aa8` on PR #21. Inspect `src/scheduling/appointment-rescheduling.service.ts`, the reservation/PATCH/finalization phases in `SchedulingService`, and `scripts/verify-appointment-rescheduling.mjs`. Confirm missing/changed replay proof cannot claim success, no intent precedes acknowledgment, and no post-acknowledgment Calendar rollback remains.
2. From the focused backend checkout run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`.
3. Run `node scripts/verify-sms-enqueue-intents.mjs` with the local Unix-socket PostgreSQL fixture setup. It creates/drops only a random disposable database, applies existing migrations there and never reads deployment credentials or invokes providers.
4. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. Review the four reschedule screenshots. Supply an installed Playwright module and optional `CHROME_PATH` elsewhere; no real token, reschedule or retry against real data.
5. In focused governance run `node scripts/docs-consistency-check.mjs`; review contracts/ticket/handoff and unchanged Now/release boundaries. This section is not ticket completion or release authorization.

## Earlier: post-Calendar cancellation intent finalization checkpoint (2026-09-08)

- Owner reviewed the retry UI and said proceed. Fetched both remotes, reconciled canonical APP-013 Now, boards, latest handoffs/ticket/contracts/evidence and focused commits. Resumed backend `9a3d86a103df675fd0f329b15922dda2ffe8736b` / governance `252f34063f3abeaaed4e4fc53b4a548a5e944dd6`. Only the existing focused feature branches were changed; original saved-checkout edits were preserved.
- Added `AppointmentCancellationService` as the local cancellation boundary. The existing pre-Calendar cancellation claim now requires the original tenant, undeleted status, calendar/window and exact job version, and returns its committed version. No intent/audit exists at this point. Only after Calendar deletion acknowledgment does finalization atomically advance that version, record the canonical `APPOINTMENT_CANCELLED` intent and write `appointment.customer_cancelled` CUSTOMER audit containing intent ID only. An explicit version advance prevents same-millisecond duplicate finalization or late compensation.
- Finalization failure after acknowledged deletion leaves the local cancellation in place and returns an office-review-required error; it does not restore an active appointment that Calendar deleted. Intent/audit failures roll back both writes. Operations-notification, worker processing and logging failures after finalization do not change cancellation success. The previous direct post-commit cancellation queue call is replaced by the durable intent path.
- Repeated cancellation requires tenant-bound finalization audit before returning success. A finalized replay makes no new Calendar call, intent or audit. Legacy/in-flight/crash-stranded CANCELLED records without that proof require office review; there is no auto-repair/backfill. Errors no longer promise a Calendar event remains scheduled when its outcome is unknown.
- Cancellation intents now use the existing worker and reviewed owner/admin retry policy. Recorded state digest, canonical queue identity, consent/quiet hours, five-failure limit and default-disabled delivery remain authoritative. Changed/restored state stops before enqueue. UI coverage copy and retry eligibility add cancellation only; reschedule is still unsupported for durable capture/recovery.
- Scope/files: new cancellation service/unit suite and disposable fixture; scheduling orchestration/module/tests; shared intent policy/recording/tests; notification intent helper/copy/fixture/screenshots; board/handoff/evidence and matching governance documents. No schema, migration, package/lockfile or provider configuration changed.

### Validation and remaining boundaries

- Backend lint/build, **42 suites / 425 tests** passed (1 suite / 3 existing policy skips), architecture and Prisma validation passed. New tests cover original-version claims, same-millisecond finalization, atomic audit/intent ordering, incompatible state, intent/audit/commit failure, post-commit failure isolation, replay proof and version-bound compensation. Initial test-double typing and formatting lint errors were corrected before the final clean run.
- `node scripts/verify-sms-enqueue-intents.mjs` passed all **14 existing migrations** in its random disposable local PostgreSQL database and invoked new `verify-appointment-cancellation.mjs`. Real Prisma/scheduling/cancellation/intent/recovery code with Calendar/operations/delivery doubles proved no intent before deletion acknowledgment, pending replay rejection, single finalized replay, disabled processing, worker deduplication, rollback after actual intent/audit insertion failures without reopening, concurrent single finalization, wrong-tenant/stale-version rejection, late compensation refusal, owner-reviewed retry, changed-state stop and no intent on Calendar failure. Earlier confirmation/technician/retry proofs stay green. **Zero provider calls**; fixture dropped and absence independently verified through `pg_database`. Existing pg concurrency deprecation warning remains.
- UI lint, **6 suites / 29 tests**, production static build (14 pages), and synthetic Chrome desktop/390px QA passed: **92 mocked requests, 78 GET/14 POST**. Failed cancellation intent is visible and reviewable only with verified owner/admin capability; all prior role/input/duplicate-click/rejection/uncertainty/actual-timeout/late-response/PII/storage checks remain green. Four new screenshots were visually inspected: `cancellation-intents-desktop.png`, `cancellation-intents-mobile.png`, `cancellation-retry-desktop.png`, `cancellation-retry-mobile.png`; browser/server closed. Fixture evidence only, not live Calendar/SMS/operator acceptance.
- Prettier, diff and governance consistency checks passed. Critical audit gates passed, **0 critical**. Findings remain backend **5 high/10 moderate**, UI **10 high/1 moderate**; dependency files unchanged. These are unresolved release risks, not accepted vulnerabilities or a clean audit.
- Important limitation: the local CANCELLED claim still precedes external deletion. Calendar and PostgreSQL are not atomic; process loss before finalization, unknown deletion results and pre-acknowledgment compensation require manual reconciliation. The retained compensation now matches the exact claimed version, but cannot prove Calendar's state. Local customer view still reflects local job status, not independently verified Calendar completion. An absent intent is not discoverable/recoverable in the notification center; legacy cancellations without the new audit are not backfilled. Rescheduling retains its direct post-commit queue gap. No end-to-end cancellation durability claim.
- No real appointment/customer data, external Calendar/message, staging/production DB, provider configuration, IAM/secrets, billing, merge or deployment action occurred. Existing intent migration requires separately approved release. Planning estimates: **APP-013 71%; governed APP-006 through APP-016 79%**, not acceptance scores. Remaining reschedule durability, calendar reconciliation, events/templates/preferences/technician notifications/email, dependency triage and owner/live acceptance. Stop for review; APP-013 stays Now.

### Exact review steps for cancellation finalization

1. Review the incremental commit after `9a3d86a` on PR #21. Inspect `src/scheduling/appointment-cancellation.service.ts`, the three phases in `cancelAppointment`, and `scripts/verify-appointment-cancellation.mjs`. Confirm no intent before Calendar acknowledgment, no reopening after acknowledged deletion, and explicit manual reconciliation/legacy replay limits.
2. In the focused backend checkout run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`.
3. Run `node scripts/verify-sms-enqueue-intents.mjs` with the local Unix-socket PostgreSQL fixture setup. It creates and drops only its own random database, applying migrations there and never reading deployment credentials or invoking providers.
4. In `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. Review all four cancellation screenshots; only intercepted synthetic data is used. Supply an installed Playwright module and optional `CHROME_PATH` elsewhere. Do not test cancellation/retry against real data.
5. In the focused governance checkout run `node scripts/docs-consistency-check.mjs`. Review updated contracts/ticket/handoff with APP-013 still Now. Approval of this section is not migration, live acceptance or release authorization.

## Earlier: guarded exhausted-intent retry UI checkpoint (2026-09-08)

- Owner reviewed the preceding retry API checkpoint and said proceed. Fetched both remotes and reconciled canonical APP-013 Now, boards, handoffs, active ticket, contracts and evidence. Started at backend `aab434b3ddcd83f6ccbf91ee6c19fb8a3f1adb3b` / governance `5f3127a7d0952913f0b0f3be7c32091b070dc0f3`; continued their existing focused branches only. No transition or marketing work.
- Added authenticated/tenant-bound private/no-store `GET /communications/sms/capabilities`, 30 requests per 60,000 ms, returning only owner/admin retry permission using the same role predicate as the POST guard. Dispatcher is read-only; other roles denied. The UI fails closed on missing/non-true/failed capability and invalidates late capability reads on session changes. The write still independently verifies current role and tenant.
- Notification center offers review only for a supported exhausted FAILED/five-attempt/unlinked intent with a canonical listed timestamp. A focused review panel displays job, intent, template and exact reviewed version; reason has no default selection, acknowledgment is unchecked, and cancellation submits nothing. Copy explains potential later SMS, consent/state gates, and that clearing a session cannot undo an accepted request.
- Submission sends one POST with acknowledgment, fixed reason code and the exact `expectedUpdatedAt`. An immediate in-flight ref prevents double clicks; the reviewed intent list and capability are cleared. Every outcome requires fresh manual loading before review can recur. Success means pending, never queued/sent/delivered. 400/401/403/404/409/429 use bounded rejection messages; 500/network/malformed success and the actual 15-second timeout show outcome uncertainty. No automatic retry. Token/job/session edits suppress late result messages without claiming server cancellation.
- Scope/files: shared communications role predicate, capabilities controller and real-guard HTTP tests; UI API/types, intent helpers/tests, notification page/panel/CSS and focused `intent-retry-review.tsx`; synthetic browser harness/four screenshots; backend board/handoff and matching governance pointer/board/ticket/contracts/route matrix/handoff. No provider service, database schema, migration or package/lockfile changed.

### Validation and limitations

- Backend: `npm test -- --runInBand` passed **41 suites / 411 tests**, with 1 suite / 3 existing policy skips. Eight added real HTTP boundary cases cover owner/admin true, dispatcher false, denied roles and missing/invalid tenant identity through synthetic Firebase verification. Build, lint, architecture check and Prisma validation passed.
- UI: lint, **6 suites / 29 tests**, and production static build passed (14 pages). Helpers reject unsupported/noncanonical/ineligible snapshots, and map rejected versus uncertain outcomes. Existing Browserslist warning remains.
- Synthetic Chrome loopback QA passed **92 mocked API requests: 78 GETs and 14 POSTs**. Checks include desktop/390px overflow, all prior history/intent regression cases, capability failure/dispatcher read-only, keyboard focus, cancel/no POST, each required input independently, exact bearer/body/timestamp, duplicate clicks producing one POST, 400/401/403/404/409/429, 500/network/malformed-success/actual 15-second timeout, and late GET/POST clearing on token/job/session edits. No page runtime errors, stored credentials or private-field rendering. Screenshots `retry-history-desktop.png`, `retry-history-mobile.png`, `retry-review-desktop.png`, `retry-review-mobile.png` were visually inspected. Browser/server closed. This is fixture evidence, not live operator/provider acceptance.
- Existing real-Prisma disposable-local proof passed 14 migrations and all confirmation/technician/recovery/tenant/FK/audit/concurrency checks with **zero provider calls**. Its random `calldesk_app013_intents_*` database was dropped; a separate read-only `pg_database` query returned no matching fixture. Existing local pg concurrency deprecation warning remains.
- `npm audit --audit-level=critical` passed in backend and UI with **0 critical**, but is not a clean dependency audit: backend **5 high / 10 moderate** (prior report 4/9); UI **10 high / 1 moderate**. Existing lockfiles are unchanged. Dependency triage/upgrade and risk acceptance remain required before release; no automatic audit fix or major upgrade was attempted within this bounded section.
- Prettier, diff and governance consistency checks passed. No production/staging action, live retry/send, provider configuration, secrets/IAM/billing, migration against an existing database, merge or deployment. Prior intent migration remains separately release-gated. Manual tokens are still a review-checkpoint UX; current role and job checks are authoritative server-side, and capability is only a snapshot. Page-local click protection does not replace cross-client server concurrency guards.
- Planning estimates: **APP-013 roughly 68%; governed APP-006 through APP-016 roughly 78%**, not acceptance scores. Remaining: owner/live acceptance, reschedule/cancellation durability, calendar reconciliation, other events/templates/preferences/technician-recipient notifications/email, and dependency release review. APP-013 remains Now; stop for owner review before another section.

### Exact review steps for this section

1. Review the incremental commit after `aab434b` on PR #21 and the four `retry-*.png` screenshots. Inspect the capability/shared role policy, `intent-retry-review.tsx`, page submission lifecycle, API timeout and helper tests. Confirm no force-state/consent override, automatic POST retry or live provider control.
2. From the focused backend checkout run `npm run -s lint && npm test -- --runInBand && npm run -s build && npm run -s arch:check && npx prisma validate && npm audit --audit-level=critical`.
3. Run `node scripts/verify-sms-enqueue-intents.mjs` only with the local PostgreSQL Unix socket/current OS user fixture setup. It creates a random disposable database, applies existing migrations there and drops only that fixture; it never reads deployment credentials or invokes providers.
4. From `ui`, run `npm run -s lint && npm test && npm run -s build && npm audit --audit-level=critical`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. Supply an installed Playwright module and optional `CHROME_PATH` elsewhere. All API responses are intercepted synthetic fixtures; do not use a real operator token or invoke retries against real data.
5. In the focused governance checkout run `node scripts/docs-consistency-check.mjs`. Review updated APP-013 boundaries and unchanged Now/paused-marketing/release gates. Approve this section only; it is not ticket completion or release authorization.

## Earlier: owner-reviewed exhausted-intent retry API checkpoint (2026-09-08)

- Owner reviewed initial confirmation durability and said proceed. Fetched both remotes and resumed backend `1ea4544` / governance `81225e0`; canonical and focused governance still name APP-013 as sole Now. Completed one API-only recovery section. Reschedule/cancellation durability, calendar reconciliation and release were not advanced.
- Added `POST /communications/sms/enqueue-intents/:intentId/retry`, restricted to verified owner/admin claims through the existing replay guard, beneath existing authentication and tenant guards. Dispatcher remains read-only. The route returns HTTP 202 `{ "status": "pending" }`, private/no-store, and uses 5 requests per 60,000 ms with the existing process-local throttler/tracker (not a distributed quota).
- Request requires `acknowledgeRetry: true`, a fixed `reasonCode` (`CONFIGURATION_REVIEWED`, `CONSENT_POLICY_REVIEWED`, or `TRANSIENT_FAILURE_REVIEWED`) and the exact canonical UTC-millisecond `expectedUpdatedAt` from the intent list. The list now includes this timestamp as an additive field. No free-text reason, arbitrary message, recipient, tenant or actor field is accepted. The server derives actor/tenant from verified context.
- Only a current `FAILED` intent exhausted at five failures, without an acknowledged queue-event link, is eligible. Missing/cross-tenant IDs are 404; stale timestamps, pending/queued/stale records, unsupported templates or missing/deleted/changed/incompatible canonical job snapshots are 409. There is no stale-intent override or force-send mode.
- A conditional update resets attempts to zero, clears the bounded error and makes the same intent pending/due. The recorded state hash and canonical identity are unchanged. The reset and `communication.enqueue_intent_retry_requested` user audit commit together; audit metadata contains IDs, template/reason codes, acknowledgment, prior failure count and reviewed timestamp only. Concurrent/repeated submissions allow one reset/audit; subsequent stale submissions return 409 and require a fresh read.
- This endpoint does not call queue processing, Calendar or SMS transport. Once deployed, re-arming authorizes the normal worker to try again when enabled; it may subsequently send if state, consent, identity and quiet-hour checks allow. It neither enables delivery nor overrides those policies. A policy-reviewed reason is not consent. Each cycle still stops after five failures; another cycle requires another fresh owner/admin review. Existing queued/dead-letter records are not replayed by this API, even if enqueue acknowledgment was lost.
- State review is a snapshot; a later job mutation is still caught by worker/queue state checks, not locked for the lifetime of external delivery. Pending/failed recovery visibility stays in the existing read-only UI; no retry button or arbitrary reset/backfill exists. Calendar/database reconciliation and absent intents remain outside this recovery path.
- Final gates passed: backend build/lint, 41 suites / 403 tests (1 suite / 3 existing policy skips), architecture and Prisma validation; UI lint, 6 suites / 27 tests and Next build (14 static pages); Prettier/diff checks. New HTTP tests use the real auth/tenant/role/validation/throttler/filter path with synthetic Firebase verification (no live identity call). They verify owner/admin 202, dispatcher/technician/viewer 403, missing/invalid identity 401, invalid UUID/acknowledgment/reason/time/extra fields 400, developer-header rejection and sixth-request 429. Service tests cover state eligibility, concurrency guard and failure privacy. One initial async-test-double lint issue was corrected before final pass.
- `node scripts/verify-sms-enqueue-intents.mjs` applied all 14 existing migrations only to its random disposable local database and now invokes `verify-enqueue-recovery.mjs`. Real Prisma proof passed wrong-tenant/stale-timestamp rejection, concurrent retry with one reset/audit, no queue event from retry alone, disabled processing, explicit fixture-worker recovery, changed-state refusal, and rollback after an injected failure following audit insertion. Earlier technician/confirmation/FK proofs remain green. Provider calls zero; fixture removed and absence verified.
- Unchanged rendered UI passed desktop/390px browser regression: 18 fixture GETs, no overflow or page errors, filters, partial failures, session/late-response clearing and private-field omission. No new rendered surface or live acceptance is claimed; previous confirmation-intent screenshots remain current. Browser/server closed.
- Critical audit passed: 0 critical; unchanged 4 high / 9 moderate transitive findings. Existing Browserslist and local pg concurrency warnings remain. No new migration, dependency, provider configuration, staging/production mutation, merge, deployment, billed action or real customer/appointment action occurred. Prior intent migration remains separately release-gated.
- Planning estimates: APP-013 roughly 65%; governed APP-006 through APP-016 roughly 77%, not acceptance scores. Remaining: recovery UI/owner acceptance, reschedule/cancellation durability, calendar reconciliation, other events/templates/preferences/technician notifications/email/live acceptance.

### Review this retry API section

1. In PR #21 inspect `sms-enqueue-recovery.service.ts`, the DTO/shared policy, owner/admin route guard and `updatedAt` list addition. Verify there is no provider dependency or forced-state/consent override.
2. Run `npm run -s build && npm test -- --runInBand && npm run -s lint && npm run -s arch:check && npx prisma validate`. Focused HTTP/service suites are `src/communications/sms-enqueue-recovery.http.spec.ts` and `src/communications/sms-enqueue-recovery.service.spec.ts`.
3. On this workstation run `node scripts/verify-sms-enqueue-intents.mjs`. It uses local PostgreSQL Unix socket/current OS user, creates a random fixture database, applies existing migrations there and drops only that fixture. No deployment credentials or provider calls are used. Confirm duplicate submissions and audit failure cannot produce extra or unaudited resets.
4. In `ui`, run `npm run -s lint && npm test -- --runInBand && npm run -s build`. Optional unchanged browser regression command remains `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. The screen must remain read-only. Live retry use, delivery enablement and release require separate approval; do not invoke this endpoint against real data for review.

## Initial confirmation intent durability checkpoint (2026-09-08)

- Owner reviewed intent visibility and said proceed. Fetched both remotes and resumed backend `09bdbbd` / governance `32cb4e5`; canonical main and focused documents still name APP-013 as sole Now. Completed initial appointment confirmation durability only, not reschedule/cancellation recovery or release.
- Added `AppointmentConfirmationService` as a focused scheduling finalization boundary. After Calendar acknowledges insertion, the matching tenant/job ACCEPTED reservation is conditionally finalized: calendar reference, `APPOINTMENT_CONFIRMED` SMS intent and `appointment.initial_confirmed` customer audit commit together. The audit stores only intent linkage; no message, phone or private management credential. Changed/deleted/cross-tenant reservations cannot finalize.
- The existing intent worker now accepts initial confirmation as well as technician departure. Both reuse state hashes, queue idempotency, leases, bounded retries and the default-disabled delivery gate. Reschedule/cancellation intent types remain unsupported; their existing direct post-commit queue triggers are unchanged.
- Successful finalization returns the booking even if immediate enqueue or operations-notification/logging fails. The persisted intent can recover missed post-commit processing without duplicating the canonical queue event; stale schedule/recipient/brand state still stops before enqueue/send under the existing checks.
- If finalization fails after an acknowledged calendar insertion, the reservation is not released or falsely returned as confirmed. The caller receives an office-review-required error. A same-window retry with no stored calendar reference returns conflict without another insert; a finalized replay returns the existing booking without new intent/queue capture. Closed/deleted requests cannot be reopened through initial confirmation.
- Explicit remaining boundary: Calendar and PostgreSQL are not atomic. A process loss after calendar insertion but before database finalization can leave a held reservation with no intent/reference; reconciliation is manual and no automated repair/control is added. A failed/ambiguous finalization does not delete the external event or release the held slot. The existing pre-acknowledgment calendar-error rollback path is unchanged and does not solve unknown provider outcomes. The intent panel cannot detect an intent that was never committed. Do not claim end-to-end appointment durability.
- Notification-center copy now accurately covers initial confirmations plus technician departures, while identifying missing reschedule/cancellation capture and backfill. No endpoint, provider configuration, schema or dependency changed. The prior intent migration still must precede any separately approved deployment.
- Final gates passed: backend build/lint, 39 suites / 362 tests (1 suite / 3 existing policy skips), architecture and Prisma validation; UI lint, 6 suites / 27 tests and Next build (14 static pages); Prettier and diff checks. New regression cases cover atomic ordering, intent/audit/commit failures, queue/logging isolation, unsupported templates, replay and unfinalized/closed/deleted rejection. Initial audit-enum/required-actor typing and formatting errors were corrected before the final clean run.
- `node scripts/verify-sms-enqueue-intents.mjs` applied all 14 existing migrations only to its random disposable local database and invoked `verify-appointment-confirmation.mjs`. Real SchedulingService/finalizer/Prisma/intent processing with calendar/notification/delivery doubles proved calendar-reference/audit/intent commit, replay, disabled processing, missed enqueue and acknowledgment-loss deduplication, stale confirmation rejection, rollback after intent insertion failure, held reservation/replay rejection and wrong-tenant finalization rejection. Existing technician recovery/FK checks also passed. Provider calls zero; fixture database removed, absence separately verified.
- Synthetic local Chrome regression passed desktop/390px no overflow, history/intent filters, empty/403/500/loading, independent partial failures, credential/job/session late-response clearing, private-field omission, keyboard order and no page runtime errors. All 18 mocked requests were GET. Updated screenshots `confirmation-intents-desktop.png` / `confirmation-intents-mobile.png` were visually inspected. This is fixture evidence, not live calendar/provider/operator acceptance. Browser/server closed.
- Critical audit passed: 0 critical, unchanged 4 high / 9 moderate transitive findings. Existing Browserslist and local PrismaPg concurrency deprecation warnings remain nonblocking. No staging/production migration, live calendar/message call, configuration, merge, deployment, billing or real customer/job mutation occurred.
- Remaining: reschedule/cancellation durability, calendar-operation reconciliation, recovery actions/policy, remaining events/template/preferences controls, technician-recipient notifications, email and live acceptance. Planning estimates: APP-013 roughly 62%; governed APP-006 through APP-016 roughly 76%, not acceptance scores.

### Review this confirmation section

1. Review PR #21's `appointment-confirmation.service.ts`, scheduling integration, confirmation intent allowlist, unit tests and local proof. Verify database finalization is atomic, post-commit failures cannot undo booking, and held reservations are not presented as confirmed.
2. Run `npm run -s build && npm test -- --runInBand && npm run -s lint && npm run -s arch:check && npx prisma validate`.
3. On this workstation run `node scripts/verify-sms-enqueue-intents.mjs`. It uses the local PostgreSQL Unix socket/current OS user, creates its own randomly named database, applies existing migrations there and drops only that fixture. It never reads deployment credentials or calls Calendar/Twilio. Review the explicit pre-finalization reconciliation gap before acceptance.
4. In `ui`, run `npm run -s lint && npm test -- --runInBand && npm run -s build`, then `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. Review both new screenshots. No live send, migration or deployment follows from this checkpoint.

## Read-only enqueue intent visibility checkpoint (2026-09-08)

- Owner reviewed the preceding durable technician section and said proceed. Fetched both remotes; backend `fab7fa6` and governance `4b61ce2` remain the latest focused checkpoint, and canonical governance main still names APP-013 as sole Now. Completed exactly one bounded UI section; appointment durability and recovery actions are not included.
- `/app/notifications` now reads existing SMS history and enqueue-intent APIs independently. The new "Before the message queue" panel shows technician departure intent status, failed enqueue attempts, bounded failure reason, claim/backoff timestamp, job/intent IDs and acknowledged queue-event ID. It does not fetch or display message content, recipient, state digest or provider identifiers.
- Pending does not promise active retry or a send time: disabled delivery, claims and backoff can all leave intent pending. Queue acknowledgment is explicitly not delivery. STALE and FAILED are stopped for review, without any send/reset/replay control.
- Intent filtering is client-side within the latest 100 tenant records, unlike history's server-side job query. Copy and counts explicitly identify this loaded subset; an empty result does not establish that no older intent exists or every job was notified. Only technician departures have durable capture; appointment intent capture and historical backfill remain absent.
- Each panel has independent loading/error/empty state. A failed history read does not hide successful intent reads, and vice versa. Token/job edits and session clearing clear both result sets and invalidate late responses. Credentials remain memory-only. No API/backend/schema/provider behavior changed.
- Validation passed: backend build/lint, 38 suites / 343 tests (1 suite / 3 existing policy skips), architecture and Prisma validation; UI lint, 6 suites / 27 tests and Next build (14 static pages); Prettier and diff checks. The explicit UI test runner now includes the four new intent helper tests.
- Synthetic local Chrome QA passed desktop and 390px no overflow, all intent status filters, loaded-subset job filtering, invalid UUID, empty/403/500/loading states, partial failure in either panel, late responses after token edit/job edit/session clear, no credential storage, private-field omission, keyboard order and no page runtime errors. All 18 fixture API requests were GET. Initial ambiguous alert test selector was scoped to its panel before the successful rerun. Browser/server closed after QA.
- Screenshots: `notification-intents-desktop.png`, `notification-intents-mobile.png` (visually inspected). Earlier history-only screenshots are retained as historical evidence. This is fixture browser evidence, not live authenticated backend/provider acceptance.
- Critical audit passed (0 critical; unchanged 4 high / 9 moderate transitive findings). Existing outdated Browserslist-data warning remains. No dependencies changed, database accessed/migrated, external send, configuration, merge, deployment, billing or real customer action occurred. Previous migration `20260908120000_add_sms_enqueue_intents` still requires separately approved release sequencing.
- Remaining: appointment durability, reviewed recovery actions/policy for exhausted or suppressed intents, other triggers, template/preferences controls, technician-recipient notifications, email and live acceptance. Planning estimates: APP-013 roughly 58%; governed APP-006 through APP-016 roughly 75%, not release acceptance scores.

### Review this visibility section

1. Review PR #21's new `enqueue-intents.tsx`, API read adapter, intent helpers/tests, page state handling and browser harness. Compare both new screenshots and check pending versus acknowledged versus delivered copy.
2. In `ui`, run `npm run -s lint && npm test -- --runInBand && npm run -s build`.
3. In `ui` on this workstation run `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs`. The loopback harness uses synthetic data only and closes its browser/server. Supply an installed Playwright module and optional `CHROME_PATH` elsewhere.
4. Verify failed intent reads leave history usable, both views clear on credential/job changes, and no recovery or sending action exists. Review intent job-filter limits: latest 100 tenant records only. No live send or deployment is authorized by this checkpoint.

## Durable technician enqueue intent checkpoint (2026-09-08)

- Refreshed both remotes; continued backend `3330c7b` / governance `b100a3e`. APP-013 remains the sole Now ticket. This section covers technician departure notifications only, not appointment outbox integration.
- Added `SmsEnqueueIntent` with tenant/job composite foreign keys, optional same-tenant communication-event linkage, unique tenant/job/template/state identity, bounded failure count and indexed due work. It stores a state digest, not message text, phone or credentials.
- A changed `on_my_way` writes its intent inside the existing job/audit transaction. If intent persistence fails, that transaction fails without committing status or audit. This is a durable-write failure, distinct from post-commit queue/provider failure, which cannot undo committed field status.
- Both immediate processing and periodic recovery retain the existing `smsDeliveryEnabled` gate. While disabled, intents persist but no recovery reads/queueing or sends occur. A conditional 60-second lease coordinates workers; expired claims are recoverable without counting a crash as a queue failure.
- Recovery passes the recorded digest to transactional queueing. Changed/deleted/incompatible state stops as `STALE`; policy/configuration/queue failures back off exponentially and stop as `FAILED` after five failures. Existing consent, encryption and pre-send stale-state controls remain authoritative.
- Queue insertion still uses the canonical lifecycle idempotency identity. A crash after insertion but before intent acknowledgment reuses the same queue record. `QUEUED` means enqueue was acknowledged, not delivered. Recovery-scan/logging failure does not block previously queued delivery work.
- Added read-only, no-store `GET /communications/sms/enqueue-intents` under existing verified owner/admin/dispatcher tenant guards, capped at the latest 100 records. It exposes operational status and event linkage but no state hash or private payload. No reset/replay control or UI change is included.
- Migration: `20260908120000_add_sms_enqueue_intents`. It must be applied through a separately approved release before this code runs against staging/production, otherwise the new transactional intent write cannot succeed. No staging/production migration was run.
- Gates passed: backend build/lint, 38 suites / 343 tests (1 suite / 3 existing policy skips), architecture and Prisma validation; UI lint, 5 suites / 23 tests/build (14 static pages), and the unchanged synthetic desktop/390px notification-browser QA. Prettier/diff checks passed. No new rendered UI is claimed.
- `node scripts/verify-sms-enqueue-intents.mjs` applied all 14 migration files to a newly created local-only fixture database. Real Prisma transactions proved atomic status/audit/intent, rollback on intent-write failure, disabled recovery, concurrent claims, ack-loss/expired-lease recovery with one queue record, stale-state rejection, tenant-scoped listing, cross-tenant job/event FK rejection and tenant cleanup. Delivery was a database-backed double, never a provider. The temporary database was dropped and absence verified; existing databases were untouched.
- Critical audit passed: 0 critical, unchanged 4 high / 9 moderate transitive findings. The local PrismaPg concurrency test emits a nonblocking pg deprecation warning; no dependency version changed. One initial enum-comparison lint issue was corrected before the final clean run.
- Remaining: appointment durable intents, operator recovery UI and policy for exhausted/suppressed intents, other events, template/preferences controls, technician notification UI, email and live acceptance. Existing pre-check/provider race remains. No historical intent backfill is attempted. Planning estimate: APP-013 roughly 55%; APP-006 through APP-016 roughly 75%, not release acceptance scores.

### Review this recovery section

1. Review schema/migration, `sms-enqueue-intent.service.ts`, technician transaction integration, expected-digest check and worker wiring in PR #21.
2. Run `npm run -s build`, `npm test -- --runInBand`, `npm run -s lint`, `npm run -s arch:check`, and `npx prisma validate`.
3. On this workstation run `node scripts/verify-sms-enqueue-intents.mjs`. It uses the local PostgreSQL Unix socket and current OS user, requires local CREATE DATABASE permission, creates a random prefixed fixture database, and drops only that database in cleanup. It never reads deployment database credentials.
4. Verify the disabled gate, no duplicate queue after acknowledgment loss, stale-state rejection and explicit pending/failed visibility. No migration, enablement, external send or release follows from this review checkpoint.

## Read-only notification center checkpoint (2026-09-08)

- Refreshed both remotes and continued backend `477048a` / governance `f9eb359`. APP-013 remains the sole approved Now ticket; no previous lifecycle work was repeated.
- Added `/app/notifications` (`SCR-APP-021`) and dispatch navigation. The screen consumes only the existing owner/admin/dispatcher SMS history endpoint: latest 100 records with optional UUID job filter, client-side status filters and counts explicitly limited to loaded records.
- Shows template label/version, job/event IDs, direction, timestamp (explicit UTC), attempts, status and bounded failure code. Sent is not labeled delivered. Failed and dead-letter records are grouped for attention; this screen has no send, replay, template-edit or email controls.
- Tokens are password-masked and held only in page memory. Editing token/job or clearing the session removes displayed records and invalidates earlier requests. Raw server errors, message bodies, phone numbers and provider identifiers are not rendered. Server tenant/role guards are unchanged.
- Backend regression gates passed: build/lint, 36 suites / 323 tests (1 suite / 3 existing policy skips), architecture and Prisma validation. UI lint, 5 suites / 23 tests and build (14 static pages) passed. Six new helper tests cover status semantics, filters, template fallback, job validation and time formatting.
- Synthetic Chrome browser QA passed against a local static export: desktop 1440px, mobile 390px without horizontal overflow, job/status filters, invalid UUID, empty/error/loading states, 403 and 500 responses, late response after session clearing, no token storage, omission of injected private fields, keyboard order and no page runtime errors. All five mocked API requests were GETs. This is fixture UI evidence, not live authenticated backend or provider acceptance.
- First browser pass exposed an ambiguous select accessible name; an explicit label corrected it. Initial CSS compatibility warnings were fixed. The existing stale Browserslist-data warning remains nonblocking.
- Screenshots: `notifications-desktop.png`, `notifications-mobile.png`. QA harness: `ui/scripts/notification-browser-qa.mjs`; it starts a loopback static server, intercepts SMS-history requests with synthetic data, then closes the browser/server. No customer database or provider call is used.
- Critical audit passed (0 critical; unchanged 4 high and 9 moderate transitive findings). No dependencies, provider settings, schemas, credentials, migrations, merges, deployments or customer data were changed. No message was sent.
- Remaining: durable enqueue recovery (history cannot reveal missing records), other event triggers, template/preferences UI, technician notifications, email and live acceptance. Planning estimate: APP-013 roughly 50%; governed APP-006 through APP-016 roughly 74%, not release scores.

### Review this UI section

1. Review the new notification page/styles/helpers, API read adapter, dispatch navigation and QA harness in PR #21. Compare both screenshots with the read-only contract.
2. In `ui`, run `npm run -s lint && npm test -- --runInBand && npm run -s build`.
3. Run `PLAYWRIGHT_MODULE=/Users/debynyhanbanks/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs node scripts/notification-browser-qa.mjs` in this workstation's `ui` directory. On another workstation supply its installed Playwright module and optional `CHROME_PATH`. Only synthetic API responses are used.
4. Verify empty history is not presented as proof of successful notification, and that no sending/replay control exists. Live operator acceptance and release require separate approval.

## Technician on-the-way trigger checkpoint (2026-09-08)

- Refreshed both remotes and resumed backend `4937330` plus governance evidence `27cb56d`; canonical governance main `af8a340` still has APP-013 as its sole Now ticket. No completed payment or appointment-trigger work was repeated.
- The existing authenticated technician status workflow now queues the fixed customer `TECHNICIAN_ON_THE_WAY` SMS only after a changed `on_my_way` action commits its job write and audit transaction. Replays with `changed: false`, rejected/stale writes and failed commits never queue a message.
- JobsModule imports the public CommunicationsModule export; the workflow does not access provider transport. Existing consent, quiet-hour, encryption, idempotency and send-time checks remain authoritative.
- On-the-way identity now includes `technicianStatusUpdatedAt`, distinguishing a later departure by the same technician from retries within one departure. Earlier queued on-the-way digests fail closed after this change; appointment message identities are unchanged.
- Queue failure emits only a bounded event code, tenant ID and job ID (no raw error, phone, token or message body). Queue and logging failure cannot reverse committed technician status.
- Validation: backend build/lint, 36 suites / 323 tests (1 suite / 3 existing policy skips), architecture check and Prisma validation passed. Fifteen new regression cases cover post-commit ordering, no-op retry, invalid/missing/stale/audit/commit failures, queue/logging failure isolation, unrelated transitions, departure identity, and current versus obsolete delivery through a provider double. Initial test-double typing lint errors were corrected before the final clean run.
- Focused technician, transactional-message and delivery suites: 3 suites / 54 tests passed. Prettier and diff checks passed.
- Unchanged UI lint, 4 suites / 17 tests and build (13 static pages) passed. No UI or route changed; no new browser screenshot or rendered APP-013 acceptance is claimed.
- Critical audit passed: zero critical, 4 high and 9 moderate existing transitive findings. No dependency, schema, provider configuration, credential, deployment or real customer/job data was changed; no external message was sent.
- Remaining limitations: post-commit enqueue is not a durable outbox. A process crash or queue failure can leave committed status without a message; status replay does not auto-repair it. Operators must inspect queue history and current state before any separately authorized recovery. The pre-send check-to-provider race also remains. Assignment/payment/dispatcher and other technician events, email, preferences, UI and acceptance remain open.
- Planning estimate: APP-013 roughly 40%; governed APP-006 through APP-016 roughly 73%. These are not release acceptance scores.

### Review steps

1. In PR #21 review `technician-workflow.service.ts`, JobsModule wiring and the departure timestamp in `transactional-message-state.ts`.
2. Run `npm run -s build`, then `npm test -- --runInBand src/jobs/technician-workflow.service.spec.ts src/communications/transactional-messaging.service.spec.ts src/communications/sms-delivery.service.spec.ts`.
3. Confirm failed commits and unchanged retries never call the queue, queue failures preserve EN_ROUTE, and obsolete departure/assignment state never reaches the provider double. Review the outbox/recovery limitations before enabling any acceptance send.

## Appointment lifecycle identity and send-time revalidation (2026-09-08)

- Added automatic fixed-template SMS queueing after a successful initial appointment confirmation, completed reschedule and completed cancellation. The trigger runs after the authoritative job/calendar operation; queue or identity failure is logged without undoing the appointment.
- Lifecycle events use `lifecycle:<template-key>:<state-digest>` as the internal idempotency source. Only its existing tenant-keyed HMAC is persisted as the queue key. The SHA-256 lifecycle digest covers template-relevant canonical job status, recipient, tenant brand/timezone, schedule or technician state without storing those raw values in history output.
- Manual operator queueing records the same digest. Immediately before provider access, all transactional SMS reload the composite tenant/job record and compare its current digest. Missing/deleted, contradictory or changed state becomes `DEAD_LETTER` with `stale_lifecycle_state`; earlier transactional events without verifiable state metadata fail closed as `lifecycle_state_unavailable`.
- Focused scheduling, transactional-message and delivery suites passed: 3 suites / 44 tests. Regression cases prove each appointment trigger, deterministic lifecycle identity, a current-state send, stale/unverifiable-message rejection before provider access, and successful cancellation despite queue failure.
- Full backend gates passed sequentially: build, lint, 36 suites / 308 tests (1 suite / 3 existing policy skips), architecture check and Prisma validation. `npm audit --omit=dev --audit-level=critical` passed with zero critical findings; 4 high and 9 moderate existing transitive Prisma/Firebase findings remain because full remediation requires breaking upgrades.
- Unchanged UI regression gates passed: lint, 4 suites / 17 tests and production build with 13 static pages. No rendered UI changed, so the existing browser evidence remains applicable and no new screenshot is claimed.
- Outbound delivery stayed disabled. No provider message, credential/configuration change, migration, merge, deployment, billing action or real customer/job mutation occurred.
- Residual boundary: revalidation happens immediately before provider access but cannot make an external send atomic with a later concurrent job mutation. Automatic assignment/technician/payment/dispatcher triggers, email, preferences, UI and staging acceptance remain open.
- Estimate: APP-013 roughly 35%; APP-006 through APP-016 roughly 72%. These are planning estimates, not release acceptance scores.

Review: inspect the lifecycle digest and pre-send validator, run build before tests, and verify a changed appointment dead-letters without calling the provider. Review the three post-commit scheduling triggers and the regression proving queue failure cannot roll back a committed cancellation.

## Message state validation checkpoint (2026-09-08)

- Continued the existing transactional-messaging branch from `9eb1b3a` after fetching both remotes. Canonical governance `af8a340` confirms APP-012 and BE-008 are accepted and APP-013 is the sole Now ticket.
- Reproduced by code inspection: the operator endpoint could select cancellation for an active job or technician-on-the-way before the stored technician status was EN_ROUTE.
- Added queue-admission checks: soft-deleted jobs return the same 404 as missing/cross-tenant jobs; cancellation requires CANCELLED; other templates reject CANCELLED/COMPLETED; on-the-way requires an assignee in EN_ROUTE; confirmation/reschedule require a persisted calendar reference and complete, ordered service window.
- Invalid states produce HTTP 409 before rendering or delivery queue access. Valid state continues through the existing consent, encryption and idempotency boundary without changing message copy or provider delivery.
- Focused service suite: 16 tests passed, including 14 new regression cases. Full backend: 36 suites / 302 tests passed, with 1 suite / 3 existing policy skips; build, lint, Prettier, architecture and Prisma validation passed.
- The first parallel test run overlapped Prisma generation in prebuild and failed loading a temporarily missing generated client. Repeating the full suite after build finished passed; run generation/build before tests in this checkout.
- Critical dependency audit passed with 0 critical; 4 high and 9 moderate existing transitive findings remain. No dependency version was changed.
- UI regression checks passed: lint, 4 suites / 17 tests and Next build (13 static pages). Installed the lockfile-defined UI dependencies locally; no manifest or lockfile changed.
- No rendered UI changed; no new browser screenshot is applicable. Existing APP-013 application screens and end-to-end messaging browser QA remain future work.
- This is snapshot validation at queue admission, not an atomic lifecycle/send guarantee. Concurrent job changes, calendar operations in progress, proof of a completed reschedule event, and stale queued messages require lifecycle integration before release.
- No external message, credential change, migration, merge or deployment occurred. APP-013 remains active and unreleased.
- Estimate: APP-013 roughly 20%; APP-006 through APP-016 roughly 70%. These are planning estimates, not release acceptance scores.

Review: inspect the service and its regression matrix, run `npm run -s build` followed by `npm test -- --runInBand`, and verify invalid state never calls the delivery queue. Review alongside the existing foundation in PR #21; lifecycle triggers and send-time revalidation remain the next bounded work.

## Transactional messaging foundation checkpoint

- Added four fixed, versioned customer SMS templates: appointment confirmed, appointment rescheduled, appointment cancelled, and technician on the way.
- Templates use the contractor tenant name, tenant-local appointment time, and assigned technician name when available. Every message includes STOP and HELP instructions.
- Added authenticated owner/admin/dispatcher `POST /communications/sms/transactional`. It accepts only a tenant-scoped job UUID, an approved template key, and an idempotency key; callers cannot submit arbitrary message text or recipient phone numbers.
- Job, customer, contractor branding, timezone, appointment time, and technician identity are loaded server-side through the existing composite tenant boundary.
- Rendered recipient and message content use the existing encrypted communication-content path. The durable event records template ID, template key, template version, job linkage, delivery state, attempt count, and bounded failure information.
- Added authenticated, no-store `GET /communications/sms/history`, optionally filtered by tenant-scoped job. Results are capped at 100 and exclude message text, phone number, recipient hash, provider identifiers, and encrypted content.
- Queue creation continues to fail closed through the existing consent, tenant identity, recipient-local quiet-hours, idempotency, retry, and global delivery-enable controls.
- This checkpoint does not automatically trigger lifecycle messages, add the operator UI, send a provider message, change configuration, or alter staging/production state.

### Checkpoint validation

- Focused communications tests: 4 suites and 21 tests passed.
- Full test suite: 36 suites passed, 1 existing policy-skipped suite; 288 tests passed and 3 existing tests skipped.
- Build: passed.
- Lint: passed.
- Architecture check: passed.
- Prisma validation: passed.
- Diff check: passed.

### Review disposition

- APP-013 remains `Now`; this is the first review-ready application checkpoint on top of the accepted BE-008 transport.
- Remaining APP-013 work includes lifecycle trigger integration, dispatcher alerts, customer/technician notification preferences, operator UI, rendered browser QA, monitoring/cost presentation, and staging acceptance.
- No migration, merge, deployment, credential access, Twilio configuration, billed provider action, or real customer message was performed.

## Bounded scope

- Signmons inbound voice and SMS transport remains available 24/7.
- Tenant business hours describe human availability and escalation expectations; they do not disable automated intake.
- Outbound SMS quiet hours are now named `outboundQuietHoursStart` and `outboundQuietHoursEnd` so they cannot be mistaken for tenant office hours.
- Existing deployments using legacy `quietHoursStart` and `quietHoursEnd` remain readable during migration.
- Equal outbound quiet-hour bounds mean no suppression and require explicit compliance review before activation.
- Outbound SMS remains disabled in staging.

## Staging correction

- Staging routes 100 percent of traffic to revision `signmons-calldesk-staging-be008bounds`.
- Liveness and database readiness returned HTTP 200.
- Unsigned Twilio voice requests fail closed with HTTP 401.
- Inbound voice and SMS remain available 24/7.
- The independent recipient-local outbound SMS quiet window is 9 PM to 8 AM.

## Validation

- Focused configuration and communications tests: 55 passed.
- Full test suite: 34 suites passed, 1 existing suite skipped; 280 tests passed, 3 existing tests skipped.
- Build: passed.
- Lint: passed.
- Architecture check: passed.
- Diff check: passed.

## Release disposition

- PR `#18` merged at `b6f1d13fb798e115eb7e8365a416a8fe07fe6123`.
- Cloud Build `a3f8fa04-5753-443d-b3e8-c4ba3b151536` produced immutable image digest `sha256:37286933d882466b8592120f48eb69c34e14cdb5a23c1f37480e39a1e6ff933a`.
- Cloud Run revision `signmons-calldesk-staging-app013bounds` passed zero-traffic liveness, readiness, unsigned voice rejection, unsigned SMS rejection, and outbound-specific configuration checks before promotion.
- The revision now serves 100 percent of staging traffic; routed liveness and readiness return HTTP 200, and the Twilio voice/SMS webhook URLs still match exactly.
- Inbound voice and SMS remain available 24/7. The separate outbound quiet window is 9 PM to 8 AM recipient-local, and outbound SMS remains disabled.
- The temporary build service account was disabled after release and retains zero project, Artifact Registry repository, and Cloud Build bucket roles.
